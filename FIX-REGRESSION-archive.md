# FIX-REGRESSION 归档（历史条目）

> 从 FIX-REGRESSION.md 归档的早期修复记录（#1–#79）。
> 平时不读，查历史先 grep 编号或功能关键词。

| # | 用户反馈问题 | 修复要点 | 验证方式 |
| --- | --- | --- | --- |
| 1 | iOS 主屏幕键盘盖住输入栏 | mobile-adapt `_ensureInputDocked` 停靠自愈 + base.css 去 min-height | `verify-ios-pwa-kbd.mjs` / 哨兵 `_ensureInputDocked` |
| 2 | iOS 保活音频嘟嘟声 | bg-keep `kaIsIOS` iOS 幅度 0.002 不可闻 | `verify-bg-notify-dedup.mjs` / 哨兵 `kaIsIOS` |
| 3 | 批量导入两行并成一个字卡 | chatcard `split(/\r\n\|\r\|\n/)` 按行拆分 | `verify-cc-batch-import.mjs` / 哨兵 |
| 4 | 表情包 GIF 变静态图 | chatcard/chat `isGif` 跳过 canvas 压缩 | 哨兵 `isGif` |
| 5 | 新文件漏接入 build.mjs（fishing/memory/my-arc） | build.mjs jsFiles 登记 | 哨兵 `fishing`/`drift-bottle` |
| 6 | 切换联系人桌面残留旧数据 | contacts `applyAvatars` 重渲染 | `verify-desk-popup-avatar.mjs` / 哨兵 |
| 7 | 信箱刷新后数据丢失 | mail `mailDbReady` 权威加载防护 | `verify-mail-isolation.mjs` / 哨兵 |
| 8 | iOS 大图崩溃（48MP/ProRAW） | personalize >8MB 拦截 + 失败不存原图 | `verify-bugfix-six.mjs` / 哨兵 |
| 9 | 关情绪字卡仍发心意/意图卡 | mood-reply 总开关总闸 | `verify-data-loss.mjs` 相关 / 哨兵 |
| 10 | 通知图标黑圈/整条丢失 | bg-keep noMedia 降级重发 | `verify-bg-notify-dedup.mjs` / 哨兵 |
| 11 | iOS Edge 弹键盘整页挤压 | mobile-adapt lockDocScroll + visualViewport | `verify-ios-kb-edge-scroll.mjs` |
| 12 | 朋友圈多回合评论只剩一条 | feed 深度合并评论 | `verify-feed-comment-merge.mjs` |
| 13 | 聊天记录重进丢失 | chat LS 快照兜底 + IDB 权威 | `verify-chat-switch-idb-timeout.mjs` |
| 14 | OPPO/雨见 搜索框打不出字 | 搜索 input 标记 ceDone 跳过 ce-box | `verify-cc-scope.mjs` |
| 15 | 音乐本地上传无法播放（夸克） | Blob + URL.createObjectURL 播放 | `verify-music-dur-cover.mjs` |
| 16 | 全屏无法关闭（OPPO Edge） | fullscreen 关闭分支先无条件退出 | `verify-desktop-mode-force.mjs` |
| 17 | 桌面点聊天被今日留言弹窗挡住 | calendar 今日留言改顶部横幅 | `verify-cal-firstuse.mjs` |
| 18 | 字卡库打开卡顿/白屏（iOS） | chatcard 分批渲染 + 去阴影 | `verify-cc-tab-totals.mjs` |
| 19 | 后台收不到消息（小米） | bg-keep 回前台 dispatch mochi-fg-resume | `verify-bg-keep-retry.mjs` |
| 20 | 数据丢失（OPPO Chrome 三连） | migrateLegacy 全局键排除 + IDB 权威 | `verify-data-loss.mjs` |
| 21 | iOS 收藏页缺条目（收藏5条只显示3条 / 提示已收藏过却没显示） | chat 收藏判重按归属+时间戳；启动回填只补不覆盖 | `verify-fav-dedup.mjs` / 哨兵 `(f.by \|\| 'me') !== 'ta'` |
| 22 | 语音播放矢量图标点击无互动变化（录制面板试听钮 + 聊天语音气泡） | 播放/暂停双 SVG + `.playing` 换暂停竖条 + `:active` 按压缩放（chat.js/group-chat.js/template.html + chat-main.css） | `verify-voice-play-icon.mjs` / 哨兵 `voice-ico-pause`、`.msg-voice-play:active` |
| 23 | 单聊联系人发消息无音效（红米 Turbo4 Pro / Via：选了内置音效不响，其他音效正常） | chat.js addIn 统一播 `playSfx('in')`（silent 与已读回执 `special:'read'` 不播）；sfx.js playBuiltin 等 AudioContext resume 完成再 start | `verify-sfx-in-chat.mjs` / 哨兵 `opts.special !== 'read'`、`p.then(start)` |
| 24 | 群聊里语音被引用时整串 base64 代码霸屏（vivo Y35 + Edge 反馈） | group-chat.js `gcQuoteHtml` 增 `gcQuoteTextSafe` 清理（与聊天页 quoteTextSafe 同构）：历史/导入数据里语音引用存的是原始「名称\|\|\|data:audio;base64…」，字符串分支直出会霸屏；对象分支的 `q.t` 同样清理 | 哨兵 `gcQuoteTextSafe` / `verify-voice-quote-gc.mjs` |
| 25 | 三星 S24 / Chrome 进聊天页卡顿后页面崩溃（旧账号大数据 OOM） | chat.js 全量 migration/去重 pass 改**分批延迟归一化**（`runDeferredNormalization` 后台 2500 条/片 setTimeout 跑）；读库后先出首屏；无本地待合并数据时跳过全量签名 Set（hasLocal 短路）——主线程阻塞从数秒降到约百毫秒 | 哨兵 `scheduleDeferredNormalization` / `diag-oom-chat-load.mjs`（seed 4万条，同步段≈107ms、无崩溃、\_\_jsErrors=0） |
| 26 | 消息长按打不开引用/操作菜单 | chat.js `msgActionEligible`+`openMsgActionsAt`：轻点保留，**新增长按**(touchstart 500ms) 弹菜单；`contextmenu`/`preventDefault` 抑制系统选中与默认菜单，`msgSuppressClickUntil` 抑制松开后误触发轻点（防弹即关）；群聊 group-chat.js `gcOpenMsgActions` 同步 | 哨兵 `openMsgActionsAt` / `gcOpenMsgActions` |
| 27 | 诊断角标显示有错误、导出却「最近错误：无」（错误记录无法保存） | device.js 错误记录双写 IndexedDB：`pushErr` 同步写 LS + `idbSet(ERR_KEY)`；新增 `readErrs`（LS 为空回退 IDB），collectDiag / refreshBadge / 已读计数统一走它；备份导入清空 `xy-home-v2:*` 键后启动时 idbRestore 自动回填，错误线索不再丢 | 哨兵 `idbSet(ERR_KEY` / 验证：记录错误→清空 xy-home-v2 前缀→刷新→诊断仍显示最近错误 |
| 28 | 已刷新到新版、顶部仍反复提示「刷新使用新版」 | pwa.js 更新条防重复：① 新增**按版本**免打扰标记 `xy-home-v2:ver-update-ack-ts`（点「刷新使用新版」/「稍后」时记为当时线上 version.json 的 ts，之后只对**比这更新的版本**再提醒——一天多次部署每次都会提醒一次，不会一天只弹一次）；② 版本轮询 + SW updatefound 两通道收敛共用 `showVerBar(onlineTs)` 跨通道一次性去重；③ SW 通道弹条前比对页面 data-build-ts 与线上 version.json ts，页面已最新则跳过（SW 交接期 reload 后新页面不再误报） | 哨兵 `ver-update-ack-ts` / `showVerBar` / 验证：刷新到新版后该版本不再弹；同日再部署新版仍会提示 |
| 29 | 聊天拍一拍面板「公用拍一拍」tab 选中态是虚线、与旁边 tab 不一致 | chat-main.css `.poke-tab-pub.sel` 去掉 `border-style:dashed`，改实心填充（与「我的拍一拍」`.sel` 同款）；dark.css 同步 `poke-tab-pub.sel` 实心（`--ink` 底 + 深字） | 哨兵 `poke-tab-pub.sel{background:var(--ink)` / 验证：拍一拍面板选中「公用拍一拍」时无虚线、与「我的拍一拍」选中观感一致 |
| 32 | 桌面美化点【恢复默认布局桌面】点了没反应（没恢复） | personalize.js 恢复默认弹窗用 `ctl.pills([{label:'确定恢复默认',value:'1'}],'1')` 预选中唯一 pill——noInput 弹窗只点底部「确定」时 fire() 传 pillVal=null → 静默不执行。与「删除美化方案」同因同修 | 哨兵 `确定恢复默认'}, '1')` / `tools/verify-desk-beauty.mjs`（只点确定 → desk-layout 清除） |
| 33 | 桌面美化的【内置壁纸预设】没应用到桌面（切 tab 后被清掉） | personalize.js `applyBgVisibility` 原来只认自定义 `phone-bg`，预设（`phone-bg-preset`）只在加载时铺一次，任何 tab 切换都把它清掉；现抽出 `bgPresetCss()`，可见性判断改为「自定义图优先、其次内置预设」 | 哨兵 `bgPresetCss` / `tools/verify-desk-beauty.mjs`（预置预设→刷新应用、切 tab 后仍在） |
| 30 | 吃什么「切换菜单」只能转盘随机，无法直接选指定菜单再用转盘抽菜 | p2-features.js 切换浮层（`eat-switch-overlay`）新增 `#eat-switch-chips` 菜单列表：点 chip 即 `eatSwitchTo(i)` 直接切到该菜单（不走随机）；转盘保留为「或转盘随机选」；chat-pages.css 增 `.eat-switch-chips`/`.eat-switch-or` | 哨兵 `function eatSwitchRenderChips` / 验证：多吃什么→切换菜单→浮层首行出现菜单 chips，点「外卖」立即切走并重画该菜单转盘，再点「转盘抽取」抽外卖菜品 |
| 34 | 聊天里联系人一直发送兜底那几条系统预设字卡（部分手机） | 双根因：① chat.js `ensureReplyCardsReady` 就绪判定用合并池（含系统默认字卡）→ 默认字卡开着时池恒非空、直接放行，挂起大键里的自定义字卡永不取回；改以「自定义字卡是否就位」为准（`hasCustomReplyCards`）。② chatcard.js `groups` 变量冷启动停在脚本加载期的空值，`applyRestored` 在「IDB 与本地数量相等」时跳过刷新 → 回复池 getter 只读得到公用字卡；新增 `replyScopeGroups` 在 getter 里检测 groups 空但 store 有数据时按需重载。群聊 `hydrateLibForCid` 已无条件取回不受影响 | 哨兵 `function replyScopeGroups` / `tools/diag-cards-hydrate.mjs` Phase E（冷启动 ensureReplyCardsReady 后 getPool 含自定义 MARKER，8/8 通过） |
| 35 | 桌面/聊天美化方案列表点【应用】只点底部确定没反应（没应用上） | 与「恢复默认桌面/删除方案」同因同修：personalize.js `applyScheme` 与 chat-settings.js `applyChatScheme` 的确认弹窗是 noInput 单 pill（「应用」），只点底部「确定」时 fire() 传 pillVal=null → `v!=='ok'` 静默不应用；现 `ctl.pills([{label:'应用',value:'ok'}],'ok')` 预选中唯一 pill | 哨兵 `{ label: '应用', value: 'ok' }], 'ok')` / 验证：美化方案→应用→只点确定→桌面/聊天立即生效并 toast |
| 36 | 档案/番茄钟等「取消/确认」型 noInput 弹窗只点底部确定没反应（删除这条/了解/疑问/暂不适用/已了解、删除描述卡、提前结束番茄） | 与 #32/#35 同因：noInput + pills + 无 `pill` 预设 → 只点底部「确定」时 fire() 传 pillVal=null → `v!=='del'/'yes'/'only'/'1'` 静默不执行。memo-arc.js delLi/delKnow/delEntry/delWonder/retireKnow/solveWonder、my-arc.js delLi/delSelf、p2-features.js `pmpQuitAsk` 均改 `pill:'del'/'yes'/'only'/'1'` 预选确认动作（删除/暂不适用/只是已了解/结束），只点确定即生效 | 哨兵 `noInput: true, pill: 'del', pills`（memo-arc + my-arc）、`noInput: true, lock: true, pill: '1', pills`（p2-features） / 验证：删除档案条目→只点确定→已删除 |
| 37 | 导出数据没有进度提示，且没弹确认框文件就直接下载了（用户还没点下载文件已保存） | data-backup.js doExport 复用 impShow 加**导出进度遮罩**（读取全部数据 n/total → 打包 → 写自动备份副本 → 准备保存），结束 impHide；saveBackupFile 删除静默自动 `a.click()` 兜底，改为返回 'blocked' 后弹「备份已打包完成」确认框，用户点「确定」（有效手势）才调 `anchorDownload(blob,fname)` 真正下载，避免"未经用户同意悄悄保存/Android 被拦截" | 哨兵 `anchorDownload` / 验证：导出大备份→出现进度遮罩、无提前下载、点确定才下载保存 |
| 38 | 设置页「复制诊断信息」点【复制】没弹窗反馈、还把浏览器网页刷新了 | 双修：① **反馈**——复制结果原本只写回弹窗顶部提示行（与打开时文案几乎相同→看不出变化）；现 device.js 改走原生 `document.execCommand('copy')`（divination.js 在用，无权限弹窗不重载）+ `window.toast` 底部气泡兜底，自动复制复/按钮复/导出都弹 toast。② **刷新**——弹窗底部 4 个按钮（复制/导出txt/取消/确定）是全站唯一漏写类型、默认 `submit` 的按钮组，个别 webview 点按会触发默认行为整页刷新；template.html 全部补 `type="button"` | 哨兵 `document.execCommand('copy')`、`type="button" class="modal-btn copy" id="modal-export"` / 验证：点【复制】出现底部「已复制到剪贴板」toast、页面不刷新；点【导出txt】出「已开始下载 txt」toast |
| 39 | 编辑聊天文字消息，发送一条新消息后，编辑内容会变回编辑前的原文（红米 K80 Chrome） | 单聊普通文字消息全部经 `buildParts` 生成 `rec.parts`，renderMsg 的 **parts 分支优先于 rec.text**。旧编辑逻辑只改 `rec.text` + 直接热替换气泡内文，`rec.parts` 仍是原文 → 一旦因发送新消息/滚动触发重渲染（renderWindow），底稿改回原文。现 chat.js 编辑确认回调内同步**重建 rec.parts**：保留非 text 段、文字段替换为新值 | 哨兵 `.filter(p => p && p.k !== 'text')` / 验证：编辑一条文字 → 发送新消息 → 编辑内容保持；再编辑一次仍显示当前值并持久化 |
| 40 | 荣耀 200 Pro Edge：系统预设字卡「朋友圈/写信使用」「我可发送语音」关掉后退出浏览器重进又变回去（Via/雨见正常）；语音开关点一次没反应要点第二次 | 双根因双修：① **写入挂起**——idbSet 无超时，荣耀/Edge 内核事务偶发挂起（既不 onsuccess 也不 onerror）→ Promise 永不 resolve、重试骨架永不触发，IDB 权威层停留旧值；加 4s 超时+重建连接重试（与 idbGet 同款）。② **杀进程回滚**——切开关后很快退出浏览器，Edge 把 localStorage 最近一次磁盘提交整批回滚；重启后 idbRestore retainValue 以「LS 有值且未标脏」为准 → 永远取回回滚后的旧值。新增**小键写日志** `__wr-journal`（xyStore.set 对 ≤64KB 值同步记 {k,v,t}，LS+IDB 双持久化，≤40 条/128KB），启动同步回放 LS 日志（先于模块初始化），restore 完成后异步合并 IDB 日志并广播 `mochi-wrj-heal`；dc-\*/cs-voice-send 开关监听 heal 重同步 UI。语音开关另去掉「与存储值相同则静默早退」守卫（回填旧值时首点被静默吃掉=点一次没反应）。诊断新增「开关持久化体检」（LS/读取/IDB 三层值+写探针） | 哨兵 `连接疑似挂起`/`__wr-journal`/`mochi-wrj-heal`×2/`开关持久化体检` / 验证：切开关→立即杀浏览器进程重开→开关保持；若复现，设置→复制诊断信息看「开关持久化体检」三层值定位 |
| 41 | 仍有手机没解决：聊天里联系人一直发送兜底几条系统预设字卡（慢/挂 IDB 手机：真我/荣耀 Edge 事务偶发挂起、MB 级字卡库读取 >8s） | 在 #34 基础上第四轮收口：① **idbHydrateKey 4s+4s 改 6s+8s**——4s+4s 对「慢但可用」的读取有害（4s 到就重建连接重开事务白费读进度，二次也只给 4s → >8s 的慢机永远取不回）；改首试 6s 耐心等慢读、仍无返回才重建连接（挂起连接重开通常当场恢复）、二次给足 8s，总上限 14s（回复等待 20s 仍兜住）。② **群聊成员回复防卡死**——hydrateLibForCid 串行取公用+专属键最坏 28s，慢机被拖十几秒像卡死；新增 `gcHydrateWait` 上限 2.5s，超时放行、下次回复再取（含撤回补发路径两处统一）。③ #34 的 `replyScopeGroups` 惰性重载已覆盖单聊 getter 空库场景 | 哨兵 `window.idbHydrateKey = function` / `tools/diag-cards-hydrate.mjs` Phase E（冷启动 ensureReplyCardsReady 后 getPool 含自定义 MARKER，8/8 通过）/ 验证：慢/低端手机重启后联系人回复不再发兜底那几条预设卡 |
| 42 | 查看存储：没怎么用就 1GB（大头是「自动备份快照」=每次手动导出把全部数据 base64 放大复制一份，约 800MB） | 【v3.29.x：本行方案已被 #86 取代——副本不再写入，存储页删除按钮已下线，改为启动自动清理遗留副本；守护目标不变：副本不得吃掉一半存储】personalize.js 查看存储页「自动备份快照」分类新增**删除按钮**（只删副本键 xy-home-v2:\_\_auto-backup-snapshot，LS+IDB 都删，真实数据全保留；openModal 二次确认→确定后禁按钮+toast，下次导出自动重建）；存储页顶部「总占用」下加警示行 st-snap-hint（仅快照存在时显示）+ 分类表该行高亮 .snap 点破"它是完整副本、删了不伤真实数据" | 哨兵已随按钮下线（现由 #86 的 `purgeLegacySnapshot` 哨兵守护同一目标）/ 历史验证：设置→查看存储→顶部警示+分类高亮→点删除→确定后快照归零、配额降约 800MB、聊天/头像/音乐无损 |
| 43 | 桌面多出一个【心愿单】入口（心愿单本是存钱罐里的功能） | p2-features.js 移除桌面「心愿单」图标（wishApp/app-wishlist 布局注入）与独立页 `page-wishlist` 全部代码（wishList/wishSave/openWishBind/openPiggyWbind/wl-*/wish-* 等），心愿功能仅保留在存钱罐页内（piggy-goals 列表 + 添加/删除/达成）；同步移除存钱罐内与独立心愿单的互绑（pg-wish/piggy-wbind）与 chat-pages.css 对应样式、contacts.js「数据互通说明」去掉心愿单（存钱目标属共用数据已列在存钱罐条） | 哨兵无（删除型改动，哨兵只查存在）；验证：桌面/第二/三页不再出现「心愿单」图标；存钱罐内心愿列表正常增删/达成；老桌面布局里的 app-wishlist 残留条目被 applyDeskLayout 静默跳过不报错 |
| 44 | 红米 K80 Chrome：音乐挂后台听，突然被停 + 弹「连续播放失败，可能是会员/付费歌曲」移出窗（用户上传的都是网易云免费歌） | 根因：Chrome 冻结后台标签/网络停顿会中断音频流触发 onerror/停滞守卫 → `offerRemoveDamagedSong` 在前台之外的瞬态失败上累计 failMap ≥2 → 把普通免费歌误判成「会员/坏链」弹窗催移出（后台停滞失败本是瞬态）。修复：① `offerRemoveDamagedSong` 开头加 `if (document.hidden) return`——后台失败不计数、不弹「移出」窗、不重置 wantPlay（真·坏链仍由前台手动播放时的 toast→连续 2 次→移出窗处理）；② visibilitychange/pageshow 回前台时 `bgResumeFails` 一并清零，保证补播封顶不挡住回前台续播 | 哨兵 `后台冻结/断流误触发 onerror，不弹「移出」窗不计数` / 验证：播放网易云免费歌 → 切后台等约 15\~60s → 回前台：不弹会员弹窗、歌曲不被移出；音乐若因断流暂停，回前台后自动续播或点播放钮即恢复；真坏链歌在前台播放仍按 toast→连续失败→移出窗流程 |
| 45 | 聊天昵称仍跟随桌面：聊天设置「联系人昵称/我的昵称」未设时右侧显示「跟随桌面（xx）」，聊天页/通话面板也回退读桌面美化昵称（用户要求彻底独立） | 聊天域昵称与桌面全面解耦：① chat.js `chatLabel` 支持 dk=null——`chatPartnerName/chatUserName` 只读聊天专用键 `cs-lbl-*`，未设默认 TA/我（v3.8 原设计），聊天顶栏未设时回退联系人名片名（非桌面美化昵称）；② 桌面来信弹窗改用 chatPartnerName；③ chat-settings.js 设置行未设时显示「未设置（默认 TA/我）」不再显示「跟随桌面（xx）」，改名清扫基线只看 cs 键；④ call.js 通话面板 partnerName/syncCallName 只读 cs-lbl-partner。桌面美化昵称（personalize bindLabel）保持原样只影响桌面 | 哨兵 `chatLabel('cs-lbl-partner', null, 'TA')` / `未设置（默认 TA）` / 通话 cs 优先 / 验证：tools/verify-chat-nick-independent.mjs（桌面设昵称→聊天顶栏/气泡/通话不受影响；聊天设置设昵称→生效并持久化；清空→回默认 TA/我） |
| 46 | iPhone 17 / Edge iOS：聊天输入栏下面空一大块（深浅模式底色跟着变）+ 页面有时突然上移点不动；点【全屏模式】进不了全屏 | ①「全屏」：Edge iOS 是 WebKit 内核，iPhone 上原生全屏不可用是平台限制（Edge「添加到主屏幕」只是快捷方式），代码已做真试一次+失败回滚+分浏览器引导，非 bug；② 空白/上移的根因是 v3.26.x 已修的「Edge iOS 实际可视高 ≠ 100dvh / 键盘会话状态残留」在部分机型仍有事件盲区：自愈只挂在 vv resize/scroll 上，底部工具条随滚动收起/展开改变可视高度时个别时机不派发 vv 事件 → `--mochi-ios-h` 停旧值（输入栏下空一大块）、平移残留无人归位（页面上移点不动）。补 window resize/orientationchange/pageshow/visibilitychange 四路触发 + 失焦态 1s 低频轮询（后台标签不跑），全部并进 scheduleHeal（rAF 合并）。另：网传「min-height:100dvh + flex + margin-top:auto 修 iOS PWA 短页面 fixed 导航飘起」与本站不适用——本站底部导航在 .phone 文档流内非 fixed，meta/apple 标签也已齐全 | 哨兵 `addEventListener('orientationchange', onIosVvEvent)` / 验证：Edge iOS 上下滑动让底部工具条收起/展开 → 输入栏始终贴可视区底部、下方无空底色块；键盘开合/切后台回来页面位置不错乱可点 |
| 47 | 每次启动报错 `def is not defined`（migrateLegacy 中断，旧键迁移不执行） | 根因：v3.26.x 新增的 pomo-\*/beauty-schemes/hide-ta-sticker 误迁移修复块在**外层 try 之外**引用了 `def`/`root`，而二者 `const` 声明在第一个 try 块内（块级作用域）→ 每次启动 ReferenceError，forEach 中断，migrateLegacy 提前 return，`__contactsMigrated` 不置位、存量旧键迁移全部跳过。修复：`def`/`root` 提升到 migrateLegacy 函数顶部（函数级作用域，两处共用），行为不变 | 哨兵 `def/root 提升到函数顶部` / 验证：无头启动 0 pageerror；启动后 `window.__contactsMigrated===true`；旧顶层键正常迁移进 default 命名空间 |
| 48 | 「TA在身边 · TA 的位置」左边的返回上一页按钮不见了 | 根因：v3.26.x 把位置面板关闭方式改为「共用左侧返回按钮、移除右侧 ✕」，但 chat-pages.css `.loc-back` 默认 `display:none`、仅 `.loc-panel.loc-full`（桌面寻踪页全屏入口）才 `display:flex`——聊天寻踪半框入口（`openLocPanel(false)` 半屏）打开后既无返回按钮也无 ✕、无遮罩点击关闭，面板完全没有关闭入口。修复：`.loc-back` 默认 `display:flex` 两种模式始终显示（删除 loc-full 专属显示规则，点击行为原本就是共用的 closeLocPanel） | 哨兵 `.loc-back {\ndisplay:flex;` / 验证：聊天寻踪半框点「TA在身边 · 看看 TA 在哪」→ 标题左侧出现返回按钮、点击回到半框；桌面寻踪页全屏进入返回按钮正常显示 |
| 49 | 吃饭提醒的夜宵时间提醒没有对应字卡，很突兀 | 根因：饭点提醒 4 个窗口（早/午/晚/夜宵 21:30–23:30）共用「提醒吃饭/追问关心」通用话术池（DEFAULT\_CARD\_DATA.eat 两组），夜宵触发抽到的还是「到饭点啦」「饭要按时吃」等"按时吃饭"语境文案，深夜发出违和。修复：① default-cards-data.js 的 `DEFAULT_CARD_DATA.eat` 新增「夜宵提醒」（10 条）/「夜宵关心」（5 条）两组，字卡库【吃什么】tab 自动显示并可逐张开关（dc-off-eat:\* 联动）；② p2-features.js `eatRemindFire` 按 `code==='nightcap'` 分别抽「夜宵提醒/夜宵关心」池（DEF\_EAT\_REMIND\_NIGHT / DEF\_EAT\_REMIND\_NIGHT\_CARE 兜底同源），其余窗口仍走通用池；夜宵话术刻意不用 `{d}` 占位，避免空菜单替换成「饭」的违和 | 哨兵 `DEF_EAT_REMIND_NIGHT` / 验证：调系统时间到 21:30–23:30 或临时把 eatRemindProb 设 100 → 夜宵窗口触发后聊天收到的是夜宵语境话术（如「夜深了，饿不饿？想吃点夜宵吗」）而非「到饭点啦」；字卡库【吃什么】tab 出现「夜宵提醒/夜宵关心」分组，关掉其中一张后夜宵触发不再出现该条 |
| 50 | 房间底部的【取消】弹窗一直不会自己消失（小米15 Pro / Chrome） | 根因：放置/移动横幅 `.r-banner` 设了 `display:flex`，会压过 UA 默认的 `[hidden]{display:none}`；`banner(null)` 里 `b.hidden=true` 加上了 hidden 但样式裁决后横幅照样显示 → 点【取消】能退出放置态（格子高亮消失）但横幅永远留在底部。修复：room.css 补 `.r-banner[hidden]{display:none}`（fish 时机条/位置面板同款坑，chat-main.css L1399 有先例注释） | 哨兵 `.r-banner[hidden]{display:none}` / 验证：进房间点家具→【移动】或家具仓→【兑换/摆放】后底部出现【取消】横幅；点【取消】或放置/移动/收回完成后横幅**应自动消失**，不再残留底部；再进屋（openRoom 清残留横幅）也应无横幅 |
| 51 | 桌面首屏「今日情话」与「已摸鱼」两张小卡片的标题行/正文行不水平对齐（小米15 Pro / Chrome） | 根因：两 mini-card 都是 `justify-content:center` 垂直居中，但内容总高不一致——情话正文 `#love-quote` 固定高 45px（3行，margin-top2），已摸鱼正文 `.mc-b` 仅单行（margin-top6），各自居中后标题/正文行位置偏移。修复：home.css 给 `.mini-card[data-card-bg="fish"] .mc-b` 与情话同构（height:45px、line-height:15px、margin-top:2px、overflow:hidden、`b` 行高继承），两卡内容等高，标题与正文首行完全对齐 | 哨兵 `.mini-card[data-card-bg="fish"] .mc-b` / 验证：`node tools/verify-desk-align.mjs` F1/F2（情话↔已摸鱼 标题行/正文行 top ≤0.6px）；桌面首屏两卡标题「今日情话」「已摸鱼」及下方正文水平对齐 |
| 52 | 小米15 Pro / Chrome：发消息/来消息/切页 2\~3s 长任务卡顿（打字/上滑/游戏中来消息被卡） | chat-msgs 在 IndexedDB 达 355MB（含图片 base64），旧逻辑每次交互都同步 `JSON.stringify(全量)`+`idbSet(全量)` → 主线程阻塞 2\~3s。修复：chat.js 持久化改空闲调度器 `schedulePersist/flushPersistNow/runPersist`（requestIdleCallback 空闲期或 ≥2500ms 合并写；仅有 `flushSave` 在离场/切页时立即落盘） | 哨兵 `function schedulePersist` / 验证：发消息→不再卡 2\~3s、来消息不打断游戏、上滑翻近段记录变顺；切页前消息不丢（flushSave 兜底） |
| 53 | 群聊与大单聊同款：每次群消息同步全量写 localstorage+IDB（group-chat-msgs 很大时同样卡） | group-chat.js `saveMsgs` 原同步 `JSON.stringify`+`localStorage.setItem`、IDB 仅 300ms 防抖；同样会阻塞主线程。修复：套用同款空闲调度 `gSchedulePersist/gFlushPersistNow`（≥2500ms 合并；`saveNow` 保留在群聊返回/清空等离场点立即落盘；补 visibilitychange/beforeunload 兜底） | 哨兵 `function gSchedulePersist` / 验证：大群聊发消息不再卡、返回群聊列表不丢最后一条 |
| 54 | 导出数据不是当前最新时间/最新聊天记录（红米 K80 Chrome：导出的聊天记录停在上次备份时刻） | 根因：doExport 先从 localStorage 把所有大键收进 data.idb，IndexedDB 循环再用 `k in data.idb` 跳过 → 聊天记录永远取 localStorage 的「有损快照」（chat.js 的 LS 快照超过 2MB 上限后不再更新、会冻结在旧时刻，且剥图/截断长文本），IDB 权威全量版（含最新消息+图片/语音）从未被导出。修复：① LS 只收录小键（≤20KB，LS 是最新同步快照）；大键记入 `lsBig` 作兜底、不提前占位 data.idb；② 大键一律从 IndexedDB 读权威值（双写键以 IDB 为准），IDB 读失败/无此键再回落 lsBig 兜底 | 哨兵 `留待 IndexedDB 权威读取` / 验证：导出→导入新浏览器→聊天记录含最新消息与图片；诊断「IndexedDB 大键明细」chat-msgs 大于 localStorage 最大键时应导出完整权威版 |
| 55 | 桌面图标长按就进「移动模式」误触：日常点开应用时按久一点（>350ms）或手指微动，图标就抖动被拖走、位置被打乱（用户要求「固定一行 4 个」） | 根因：非移动模式下长按图标 350ms 自动 `enterMoveMode`+`startDeskDrag`，日常点击/浏览极易误触进入移动模式并被拖乱。修复（personalize.js）：**删除长按自动入口**——非移动模式 pointerdown 直接 return，日常长按无副作用（点击照常）；拖动排序仅保留主动入口：装饰模式（设置→自定义桌面图标）→「编辑布局」→ 移动模式（短按即拖）。网格本身已是固定 4 列（`.app-grid` repeat(4,1fr)，三页统一） | 哨兵 `pressTimer = setTimeout(() => {\npressTimer = null;\nenterMoveMode();\nstartDeskDrag(e, t);`（**absent**，删除型哨兵：产物中出现即报警；不能用短 needle `pressTimer = setTimeout`——chatcard.js 字卡库长按排序同变量名会误报）/ 验证：桌面任意图标长按 3s 不动 → 不进移动模式、不抖动、位置不变、仍能正常点击打开；设置→自定义桌面图标→「编辑布局」→ 短按图标仍可拖动换位并持久化 |
| 56 | 经期温柔动作后缀字卡只写进 1 条：字卡库→系统预设字卡→其他互动功能字卡→经期 里只有「（轻轻抵着你的额头）」一条，其余 5 条动作后缀（把你往怀里带了带/握紧你的手/摸了摸你发顶/语气柔下来/把热牛奶推到你手边）搜不到也关不掉（v3.14.x 只把 WARM\_SUFFIX 的一条登记进字卡库） | 根因：period.js `WARM_SUFFIX` 六条动作后缀抽取侧一直按 `isDefaultCardOff('period', x)` 过滤，但展示侧 `DEFAULT_CARD_DATA.period` 只登记了「（轻轻抵着你的额头）」一条且混在「经期关心」分组末位（还会被 PERIOD\_CARE\_LINES 当关心语抽取发出）。修复：default-cards-data.js 将其移出「经期关心」，新增「温柔动作」分组写全 WARM\_SUFFIX 六条（与 period.js 同源，开关键即文案本身）；「经期关心」恢复纯 20 条关心语 | 哨兵 `（把你往怀里带了带）` / 验证：字卡库→其他互动功能字卡→经期 tab 见「经期关心」（20 条）与「温柔动作」（6 条）两个分组；任一动作后缀关闭后 warmSuffix 不再随机拼出（`node tools/verify-warm-suffix.mjs` 如建）；经期关心触发不再发出「（…）」动作文案 |
| 57 | 华为 Mate 40 Pro+ 自带浏览器：①编辑布局里拖动图标只能竖着换行、横着放不了；②点【恢复默认桌面】没恢复 | 双根因双修（personalize.js）：① **横滑被抢成翻页**——移动模式 pointermove 里短按后「横向位移为主（\|dx\|>\|dy\|*1.5）→ 判横滑翻页（deskGo）不拖拽」，图标横向拖动永远进不了拖拽，只能上下换行；移动模式翻页本可由「空白处原生滚动（.desk-move-mode 容器 touch-action:pan-x pan-y）+ 拖到屏幕边缘自动翻页」承担，JS 横滑翻页冗余且有害 →* ***删除横滑翻页分支，图标/组件上任意方向移动都 startDeskDrag**（clone 横/竖都跟手）。②* ***恢复默认 400ms 后 reload，IDB 删除未落盘**——store.remove 里的 idbDelete 是异步 fire-and-forget，IDB 慢/事务挂起的浏览器（华为）删除未提交就刷新，新页面 idbRestore 把旧 desk-layout 从 IDB 回填回来 → 布局没恢复；改为显式* *`idbDelete`* *完整键（activePrefix 命名空间：desk-layout/hidden-icons/app-icon-order-*）**等删除完成（每键 3s 兜底）再 reload**（store.remove 已同步清 LS+memory+journal，wrjForget 防日志回放旧值） | 哨兵 `Math.abs(dx) > Math.abs(dy) * 1.5`（**absent**，横滑翻页已删）、`idbDelete(P + ':desk-layout')`（存在，恢复默认落盘）/ 验证：tools/verify-desk-longpress.mjs 场景4（横向拖动出现 clone 且跟手、不翻页）+ 场景5（恢复默认后 LS+IDB desk-layout 均无，reload 后不回填）17/17；verify-desk-beauty 14/14；verify.mjs 10/10 |
| 58 | 系统预设字卡搜索框搜不到经期温柔动作字卡：在字卡库任意搜索框搜「轻轻抵着」都无结果（字卡其实在「经期」tab 下，但搜索只限当前 tab，跨页面更搜不到） | 根因：default-cards.js `render()` 搜索只过滤 `DATA[view.cur]`（当前 tab 的分组），其他 tab/页面的字卡完全不在搜索范围。修复：① `mountCardView` 新增 `searchKeys`（dc/fc 两页均传 `ALL_KEYS`=BASE+FUNC 全库）；② 搜索时跨全库匹配（文案或分组名含关键词），分组头标注来源「\[tab名] 分组名」（TAB\_LABELS 从 dc/fc tabs 的 data-type→显示名映射）；③ 跨 tab 搜到的字卡开关按**真实分类**落库（`setCardOff(rec.cat)`，避免误存到当前 tab 的 dc-off-<cat>） | 验证（tmp 脚本 9/9，已删）：经期 tab=经期关心20+温柔动作6；功能页搜「轻轻抵着」命中；默认字卡页搜「往怀里」跨库命中并标注 \[经期]；跨库关闭字卡 → `dc-off-period:（把你往怀里带了带）`=1（LS+store 双写），重开恢复 0 |
| 59 | 桌面美化【导入美化方案】从文件导入选了 .json 依旧没反应、没应用（txtImportAuto 修过仍无效） | 根因：`opts` 是 `window.openModal` 的**函数参数**（函数体在 `return ctl;` 处结束），而文件导入的 change 监听器定义在 **IIFE 作用域**——`if (opts.txtImportAuto)` 直接抛 **ReferenceError: opts is not defined**，onload 在 `textarea.value = txt` 之后中断 → fire()/close() 永不执行 → 弹窗不关、数据不应用（此前 txtImportAuto 修复从未真正生效）。修复：IIFE 级新增 `_modalOpts` 变量，`window.openModal` 每次打开时 `_modalOpts = opts`，change 监听器改读 `_modalOpts.txtImportAuto` | 哨兵 `_modalOpts`（存在）/ 验证：tools/verify-desk-longpress.mjs 场景6（DataTransfer 注入 .json → 数据应用 page-bg-0/__accent__ + 弹窗自动关闭）22/22；verify-desk-beauty 14/14；verify.mjs 10/10 |
| 60 | 桌面美化【导出美化方案】有已保存方案时选了「当前设置/某个方案」点确定后看不到导出方式（导出文件/复制文字）弹窗，「导出没反应」 | 根因：**嵌套弹窗被外层确定按钮的 finally close() 立即关掉**——弹窗A（选导出来源）的 cb 里同步 `startBeautyExport` 打开弹窗B（导出方式），但 okBtn 的 `try { fire(); } finally { close(); }` 在 fire 返回后把刚打开的弹窗B 关闭；`ctl.stay()` 也救不了（openModal 每次打开重置 stayOnce）。修复：**弹窗打开序号守卫**——IIFE 级 `_openSeq`，openModal 打开时 `_openSeq++`；okBtn/Enter/pillSubmit 的 finally close() 改为「`_openSeq` 未变化才 close」（fire 期间开了新弹窗 → 跳过关闭，新弹窗保留）。同步给 startBeautyExport 的 clipboard 不可用 fallback 分支加 ctl.stay()（保险）。此守卫同时修复所有「弹窗A确定后开弹窗B」类嵌套场景 | 哨兵 `_openSeq`（存在）/ 验证：tools/verify-desk-longpress.mjs 场景7（导出→导入闭环：有方案时选「当前设置」→ 导出方式弹窗正常出现 → 复制文字得 JSON → 该 JSON 注入导入可应用恢复 page-bg-0）28/28；verify-desk-beauty 14/14；verify.mjs 10/10 |
| 61 | 导出美化方案选「复制文字」得到的是含图片 base64 的超大 JSON（壁纸/自定义图标/图片组件），剪贴板/聊天工具复制发送会被截断或失败，对方也无法粘贴导入（用户质疑"复制文字不正确"） | 根因：`startBeautyExport` 的 text 分支无条件把整个 JSON（含 phone-bg/app-icon-*/desk-image-src-* 等 dataURL，一张壁纸 1MB+）写入剪贴板或 fallback textarea。修复：**复制文字前检测 JSON 大小**——`json.length > 512*1024`（含壁纸/多图的典型大小）时 toast 提示「方案含图片，文本过大，请用『导出文件』分享 .json」并 return（不复制、不弹 fallback）；导出弹窗 staticText 同步注明「含壁纸/图标图片的方案请用导出文件，复制文字仅适合无图片的轻量配置」 | 哨兵 `json.length > 512 * 1024`（存在）/ 验证：tools/verify-desk-longpress.mjs 场景7 追加段（塞 600KB 壁纸 dataURL → 导出 → 复制文字被拦截：弹窗关闭、无 fallback 文本）29/29；verify-desk-beauty 14/14；verify.mjs 10/10 |
| 62 | 用户要求：导入/导出美化的「可复制文字」直接删掉，改为只能文件 | 按需重做（personalize.js）：**导出**——移除「导出文件/复制文字」二选一弹窗与 `showBeautyFallback`/剪贴板路径，选完来源（当前设置/方案）直接 `downloadBeautyFile` 下载 .json（文件无大小限制，含图片方案不受影响）；**导入**——弹窗改 noInput（隐藏 input/textarea），只保留「从文件导入」+ `txtImportAuto` 选文件即应用；openModal 的 txtImportAuto onload 从 `fire()` 改为**直接** **`cb(txt)`**（noInput 弹窗下 fire() 会传 'ok' 导致 JSON 解析失败）；导入回调加 `v==='ok'` 守卫（未选文件点确定时提示「请点击『从文件导入』选择 .json 文件」）。注意：chat-settings.js 另有同形「复制文字」功能（聊天域），不在此次移除范围 | 哨兵 `function showBeautyFallback`（**absent**，防「复制文字」加回；不能用 `{label:'复制文字',value:'text'}`——chat-settings.js 同形串会误报）/ 验证：tools/verify-desk-longpress.mjs 场景7 重写（选来源确定后弹窗关闭直接下载、无「复制文字」选项、大 JSON 也能导出、导入弹窗无输入框仅文件按钮）28/28；场景6 文件导入自动应用仍过；verify-desk-beauty 14/14；verify.mjs 10/10 |
| 63 | 小米15 Pro Chrome：导出和导入数据不包括信箱数据（含图片信件 >200KB 后信箱主键被 xyStore 从 LS 删除只留 IDB→导出找不到→丢失） | 根因：FIX-REGRESSION #54 修复将导出策略改为"LS 只收小键（≤20KB）+ IDB 权威读大键"后，>200KB 的信箱主键 `mail-letters` 已被 xyStore 从 LS 删除（第 356-367 行）→导出 LS 阶段遍历不到→无 `lsBig` 兜底 → IDB 事务挂起/超时读取失败后无任何回退路径，该键被静默跳过。修复（data-backup.js）：① IDB 读取失败且 `lsBig[k]` 为空时重试一次 IDB 读取；② 重试仍失败时最后尝试 `localStorage.getItem(k)` 直接读取（LS 可能有残留快照） | 哨兵 `IDB 读取失败且 lsBig 无兜底` / 验证：导出后查看备份 JSON 的 `data.idb` 中是否含 `mail-letters` 键；含图片的信箱数据导入新浏览器后恢复完整 |
| 64 | 小米15 Pro Chrome：导出数据功能没有确认显示导出了哪些功能的数据；体积只显示 KB 不显示 MB | 修复（data-backup.js）：① 新增 `exportCoverage(data)` 按键尾统计各功能模块（聊天/群聊/信箱/朋友圈/字卡/收藏/头像/音乐/占卜/日历/备忘/群资料/经期/记账/花园房间/漂流瓶/桌面布局）本次导出数量/是否有数据；② 新增 `fmtSize()` 体积友好显示（<1024KB 显 KB、≥1MB 显 MB、更小显 B），导出 toast/标题/弹窗统一替换原 `sizeKB + ' KB'`；③ 导出确认弹窗「备份已打包完成」staticText 顶部插入「导出内容（全局全部数据）：」+ 功能覆盖清单，向用户明确"导出=全局全量数据（localStorage 小键 + IndexedDB 大键）" | 哨兵 `导出内容（全局全部数据）` / 验证：导出→弹窗标题显示 MB 级体积（若超 1MB）且 staticText 列出各功能数据条数；大备份 toast 显示 `x.x MB` 而非 `xxxx KB`；导入侧 backupSummary 仍正常 |
| 65 | 旧数据（聊天含图片，chat-msgs 单键几十\~几百 MB）多的用户每次打开都弹「存储异常：近期数据多次写入失败」，实际数据并无丢失 | 根因（idb.js）：①`_idbFailCnt` 整个会话累计不清零——iOS 回前台连接重建/偶发抖动的一阵失败永久污染计数，之后哪怕全部写成功也照样弹窗（每会话一次→用户感知"总是显示"）；②v3.26.x 引入的 4s 事务挂起超时不分体积——慢设备上合法大包写入本就可能 >4s，被判失败→重试又超时→计 1 次失败，事务实际多半最终写成功，纯误报；数据越大越必现。修复：①任一次 idbSet 成功即 `_idbFailCnt=0`——只对「连续失败」告警（配额满等真实持续失败仍会连计 5 次照常弹，不掩盖问题）；②写入超时按值体积放大：字符串 >256KB 起每 256KB +2s、封顶 +26s，小值维持 4s 快速判挂起（荣耀/Edge 挂起场景不回归）；③console.warn 附最后错误名便于报障定位 | 哨兵 `成功即清零——只对连续失败告警`（存在）/ 验证：node --check 过；verify-data-loss 11/11；布局 verify 10/10；上线=a1858f7（18:41 产物含修复特征与 262144 放大常量）。注意：verify-chat-switch-idb-hang 5/9 与 -timeout 10/11 的 5 个 FAIL 为**既有问题**（新旧产物一致复现，chat.js LS 快照/渲染域，疑 a20df1d 引入），与本次无关，待 chat.js 域排查；报障侧指引：弹窗若修复后仍出现=真实持续写入失败（多为配额满），引导导出备份+清理浏览器存储+回传「复制诊断信息」 |
| 66 | 聊天设置改「联系人昵称」刷新后聊天顶部回退到联系人名片名（修改前昵称），新昵称不生效 | 根因（chat.js）：mochi-restore-done 监听器漏调 updateChatPartnerName()，且未监听 mochi-wrj-heal。LS 写失败（配额满/隐私模式）时刷新后 memoryCache 空→同步读 LS 拿旧值/null→回退 contacts.name；idbRestore 随后用 IDB 新值回填 memoryCache（脏键以 IDB 为准），但聊天顶部不重渲染→新值永不显示。chat-settings.js 有这两处兜底，唯独 chat.js 漏了 | 方案：①mochi-restore-done 内补 updateChatPartnerName()；②新增 mochi-wrj-heal 监听重调。验证：node --check 通过；待 build 后真机验证（改昵称→刷新→顶部保留新值） |
| 67 | 有的手机上系统预设字卡里的颜文字显示成叉叉（空心方框/框内叉），没有正常显示 | 根因：颜文字是普通文本（希腊/泰文/阿拉伯文/西里尔、数学符号、制表符等罕用 Unicode 字符），显示依赖手机系统字体，字体缺字形的字符渲染为 .notdef 占位（方框/叉叉）；不同品牌机型字体覆盖不同→同字卡有的手机正常有的叉叉。修复：tools/fix-kaomoji-chars.mjs REPLACEMENTS 第二批——ᴥ U+1D25→ω、≖ U+2256→￣、◍ U+25CD→◎、₃ U+2083→3、❛ U+275B→˘，共替换 14 处（含残留孤立代理 U+DC40×2）；首批（ᴗ→ω 等）沿用 | absent 哨兵：产物不含 ᴥ（build.mjs FIX\_SENTINELS，71/71）/ 工具自校验 DEFAULT\_CARD\_DATA 加载成功（kaomoji 43 组 496 张）/ npm run verify 10/10 / 真机：原显示叉叉机型看颜文字不再有方框 |
| 68 | 聊天里发送拍一拍：联系人的昵称变成 他/ta/她（受称呼功能影响）；且拍一拍不显示「我」和联系人的昵称，人称不对 | 根因（chat.js）：①sendPoke 把「你」替换成 chatPartnerName() 字面昵称（聊天昵称未设时=占位 TA），而拍一拍消息按 side:'in' 渲染会过 taFit（称呼功能），文本里的 TA/他/ta 被整体替换成称呼词 → 昵称槽位变成 他/ta/她；②sendPoke 前缀用字面「我」而非 chatUserName()，我的昵称永不显示（联系人侧 performPoke 反而已用双昵称，两边不一致）。修复：①sendPoke/performPoke 改存 {me}/{ta} 占位符（不再落字面昵称，改名后历史拍一拍自动跟随）；②renderMsg 的 T() 重排——taFit 期间把 {ta}/{me} 掩成控制符（\u0002/\u0003），先跑称呼替换再回填昵称，昵称永不被称呼改写；字卡文案里独立的 ta/TA/他（非占位符）仍按称呼替换（字卡库中性占位设计不变，见 #c9703a6） | 哨兵 `const hasPh = t.indexOf('{ta}') >= 0 \|\| t.indexOf('{me}') >= 0`（build.mjs FIX\_SENTINELS）/ 验证：node tools/verify-poke-nick.mjs 7/7（称呼=她时「拍了拍你」→我 拍了拍TA 不再变她；设昵称阿红/小明后「拍了拍你的脸蛋」→阿红 拍了拍小明的脸蛋；「摸了摸ta的头」仍跟随称呼→她；称呼改回不设置中性保留）；verify-ta-gender 22/22（2 处过期期望对齐 v3.23.x ta 占位数据，非回归） |
| 69 | 打砖块小游戏：进行中切换球数（如切 2 球）无效；有进行中对局时重新打开面板只能「继续」，无法放弃旧局重新选球（"开启无法选多个球"） | 根因（breakout.js）：①球数切换只在「下次发球/补发」时生效（change 只存 localStorage 偏好），进行中（rally）切 2 球场上不补发、切 1 球不剪除 → 玩家感知"无效"；②进行中对局重开面板走 resume 分支（只给「继续」，副按钮隐藏），旧局不结束就永远开不了新局、选球数永远不生效。修复：①change 时若在 rally 中**立即调整场上球数**——少于目标立即 launchBall 补发、多于目标从数组尾部 pop 剪除（恒不动 balls\[0] 维持 s.ball===s.balls\[0] 不变量；剪掉的球若是 aiBall 则清空，planDescent 下帧自动重选）；serve/clearing 态仍由下次发球按新数生效；②resume 分支副按钮显示「新开局」（点击放弃旧局 startGame，球数/难度按当前选择），endGame 时副按钮文字重置回「返回小游戏」；③选择框 title 改「球数量（随时调整）」 | 哨兵：`while (state.balls.length > target) {` + `overlayCloseBtn.textContent = '新开局'`（build.mjs FIX\_SENTINELS）/ 验证：node tools/verify-brick.mjs（新增 T-B4a/b 进行中切 2 球立即补发、T-B5a/b 切 3 补足/切 1 剪除+aiBall 无悬空、T-B6a/b 重开面板副按钮「新开局」放弃旧局按 2 球开局）全绿 + 全程无 JS 异常；真机：玩的时候切球数立即变、重开面板可点「新开局」重新选球 |
| 70 | iOS 15 Pro Max 手机所有浏览器打开 GitHub Pages 链接：开屏一直自己刷新然后白屏，完全打不开无法使用 | 根因（build.mjs + sw\.js）：①**产物主脚本单块达 2.85MB**——iOS 15 WebKit(615)/JavaScriptCore 对超大单块内联 script 解析触发内存限制 → WebContent 进程崩溃 → Safari「此页面出现问题」自动重新加载 → 每加载必崩 → **无限刷新循环 → 白屏**（iOS 上所有浏览器都是 WebKit 内核，故"所有浏览器"现象一致）；②GitHub Pages 国内访问慢（实测 \~30KB/s），sw\.js 网络优先 3.5s 必超时，若预缓存失败/旧缓存被清，导航回退命中空缓存 → `Response.error()` → 白屏。修复：①**build.mjs 把主 bundle 按 ≤600KB 拆成多个** **`<script>`** **块**（块间保持 jsFiles 顺序、每文件仍是独立 IIFE+try/catch，全局共享不受影响；单块远小于 iOS 15 解析上限）；②**sw\.js 导航兜底**：网络超时且缓存为空时改发不带超时的 `fetch(req)`（成功照常写入缓存），不再直接 `Response.error()` 白屏；SW 内部 fetch 不会再次触发本 SW 拦截，无死循环风险 | 哨兵 `</script>\n<script>`（产物拆块，build.mjs FIX\_SENTINELS）/ 验证：node tools/verify-ios15-white-screen.mjs 10/10（产物 script 块数 ≥3 且每块 <700KB、关键功能在位、sw\.js 含缓存空兜底 fetch(req) 且无 Response.error 白屏）；主 bundle 全部块 node --check 通过；WebKit 加载 `__mochiDataReady=true`；npm run verify 10/10；真机：iOS 15 打开不再无限刷新白屏，首次慢加载后再次打开秒开（SW 缓存） |
| 71 | 新增功能：音乐「TA 暂停再播放」互动（用户要求：小概率触发 + 聊天发字卡 + 字卡进系统预设【其他功能字卡】；且不要"一直暂停又继续"的循环，概率可调、可关闭 TA 暂停权限） | music-player.js 播放中按 `taPauseProb`（默认 3%，音乐设置步进器 0~~100 可调，设 0 关闭）掷骰子：命中则播放 10~~25s 后 TA 暂停 → 聊天发「TA 暂停播放」字卡 → 3.5s 后 TA 点播放恢复 → 发「TA 恢复播放」字卡（保留进度）；**权限总开关** **`taPauseEn`**（音乐设置「联系人可暂停你的播放」，关闭=彻底不触发 + 步进器置灰）；**防连发三重守卫**：①同歌只互动一次（taPauseDoneId）②互动后冷却 cooldownMs 默认 10 分钟（taPauseCooldownAt，连续切歌也不会每首都触发）③互动进行中 taPauseActive 全局重入保护 + onpause/tryResumePlayback 补播短路；用户手动播放/暂停/切歌/来电即取消；字卡在 default-cards-data.js `DEFAULT_CARD_DATA.music` 两组，字卡库【其他互动功能字卡 → 音乐】tab 逐张开关（dc-off-music:\*，全关回退内置兜底） | 哨兵 `taPauseProb`、`taPauseEn`（music-player.js）、`TA 暂停播放`（default-cards-data.js）；tools/verify-ta-pause.mjs 23/23（静态含开关/防连发键）+ tools/verify-ta-pause-live.mjs 15/15（行为链路：暂停字卡→3.5s 恢复字卡→恢复播放；L6 互动后 8s 无第二条暂停字卡=防连发；L7 taPauseEn=false 播放 12s 全程未暂停无字卡=权限关闭生效）；真机确认点：音乐设置见「联系人可暂停你的播放」开关 +「播放中·TA 暂停再播放概率」步进器，关闭开关后播放不再被 TA 暂停；默认概率下听歌偶发一次暂停-恢复互动，不会连发 |
| 72 | 红米K70 QQ浏览器：无法收藏联系人互动的卡片，有的可以收藏、有的点击收藏无效（心形在但不弹已收藏） | 根因（chat.js）：① `cardSnapshot` 只覆盖 ask-choose/ask-curious/ask-roast/ask-card/invite 五类，但 renderMsg 对 ask/redpacket/flower/gift/dish 也渲染了收藏心形（favHeartHtml）→ favCardFromMsg 走 cardSnapshot 得 null 直接 return，无 toast 无入库；② 心形点击处理 `favBtn.closest('.msg-ask')` 只认 .msg-ask 家族，红包(.msg-rp)/送花(.msg-flower)/礼物(.msg-gift)/佳肴(.msg-gift.msg-dish) 容器定位不到 → 即便快照修好也点不动。修复：① cardSnapshot 补齐 ask（q=askQuestion/mine=askAnswer/ta=askReply）、redpacket（红包¥金额+祝福）、flower、gift、dish 五类的 q/mine/ta 快照；② 心形 locator 改 `favBtn.closest('[data-idx]')`，并给 flower/gift/dish 渲染补 `data-idx`；③ 收藏夹 FAV\_KIND\_LABEL 补 ask/redpacket/flower/gift/dish 的友好标签 | 哨兵 `favBtn.closest('[data-idx]')`（build.mjs FIX\_SENTINELS，存在）/ 验证：node --check src/js/chat.js 过；真机：问TA/红包/送花/礼物/佳肴等互动卡点心形均应 toast「已收藏互动卡片」并进收藏夹（此前红包/送花/礼物/佳肴/问TA 静默无效） |
| 73 | iPhone XS Safari（iOS 18.5，PWA standalone）：头像互动添加头像图片后，导出数据一导出就闪退（WebContent 进程崩溃退出） | 根因（data-backup.js）：doExport 末尾 `idbSet(SNAPSHOT_KEY, json)` 把整个导出 JSON（含 chat-msgs 13.8MB + 头像 dataURL，可达 15~25MB）再 structured clone 一份写 IndexedDB 副本键，内存峰值翻倍。iOS Safari PWA standalone 下 WebContent 进程内存限制严，大备份写副本时 Jetsam 杀进程 → 闪退。添加头像后数据量越过阈值。修复：`json.length > 3MB` 时跳过副本写入（小备份仍写副本保留"数据丢失自动恢复"能力，大备份用户本就持有导出文件） | 哨兵改为 absent 守卫 `idbSet(SNAPSHOT_KEY`（v3.29.x 起导出**彻底不写副本**，见 #86——比原「>3MB 才跳过」更彻底，本行闪退根因直接消除）/ 验证：node --check 过；真机：添加头像后导出不再闪退，导出文件完整含聊天记录+头像 |
| 74 | iPhone XS Safari（iOS 18.5）：自定义字卡所有页面（公用/专属）添加字卡后"卡卡的点不动" | 根因（chatcard.js）：批量导入 cb（2040 行）和 finishUpload（1979 行）直接 `saveGroups(groups)` 同步序列化整个字卡库（含表情包/图片 dataURL 可达几 MB~几十 MB）阻塞主线程，与编辑单卡 openEditCard 已用 scheduleSave 延后的做法不一致（注释 578-581 行已明确"saveGroups 序列化大库会卡住确认"）。修复：两处 `saveGroups(groups)` 改 `scheduleSave()`，持久化延后 120ms，render() 立即用内存 groups 渲染 | 哨兵 `持久化延后——同批量导入`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；真机：添加字卡后不再卡顿，字卡立即出现，持久化延后完成 |
| 75 | iPhone17 Edge 浏览器 PWA：桌面图标按钮显示不全缺很多；且开屏进入用一会会退回旧版本然后白屏 | 根因①（base.css）：.ios-pwa-standalone .phone height:100vh，iOS standalone 下 100vh=screen.height（含被隐藏状态栏区），ios-fs-active（已隐藏模拟状态栏）下可视视口=innerHeight，.phone 超出视口 → body 可滚、桌面顶/底各被裁（图标被裁）。修复：加 .ios-pwa-standalone.ios-fs-active .phone height:var(--mochi-ios-h,100dvh)（syncVvFit 实测 visualViewport.height），填满可视区不溢出。根因②（sw.js）：导航回退 caches.match('./index.html') 不指定 cacheName，所有缓存找「第一个」（顺序未定义，旧缓存可能先命中旧版）→ 即使当前 CACHE 有新版也回退旧版；叠加 iOS PWA 切后台/回前台 WebKit 重新加载 + 网络超时回退 →「退回旧版白屏」。修复：导航回退改 caches.open(CACHE).match 优先当前 CACHE；activate claim 后异步补 fetch index.html 写入当前 CACHE（precache 失败自愈） | 哨兵 `.ios-pwa-standalone.ios-fs-active .phone`（base.css）+ sw.js 专项哨兵 2/2（build.mjs）/ 验证：node --check 过；npm run verify 10/10；真机：iPhone17 Edge PWA 桌面图标完整显示、切后台回前台不再退回旧版白屏 |
| 76 | 红米 K80 Chrome：播放音乐浏览器挂后台总是自己停止播放；通知栏里之前有播放音乐和后台保活的弹窗，现在没有了（时有时无） | 根因：通知栏媒体条由唯一 MediaSession 驱动，music-player 与 bg-keep 抢同一个会话。音乐后台被外部打断（省电/音频焦点/断流）瞬时暂停 → onpause 标 playbackState='paused' + __musicPlaying=false → bg-keep watcher 立即 syncKeepForMusic → 保活音频起播 + setKeepMediaSession **把歌曲元数据覆盖成「Mochi 后台保活」**；300ms 后 bg-resume 恢复播放，onplay 只标 'playing'、**从不恢复歌曲元数据** → 媒体会话仍绑定在被暂停的保活元素 → Chrome 判页面非"播放中" → 媒体条消失、标签页冻结 → 音乐停播（偶发触发故"时有时无"，一旦触发坏到下次完整 startPlayback）。次要：tryResumePlayback 连续失败 ≥6 次永久放弃，后台弱网停后无人拉起。修复：①music-player 把 wantPlay 暴露为 window.__musicWantPlay（外部打断=还想播）；②bg-keep setKeepMediaSession 加 musicIntentPlaying() 让位守卫——音乐还有播放意图时不覆盖歌曲媒体条（保活音频照常出声防冻结）；③audio.onplay 追加 updateMediaSession(true)——每次真正出声重设歌曲元数据 + 'playing'，会话重新绑定到在播的音乐元素；④audio.onpause 按意图标状态：外部打断保持 'playing'，仅用户主动暂停标 'paused'；⑤tryResumePlayback 连续失败封顶改 60s 冷却后清零重试（bgResumeFailAt），后台不再永久停播 | 哨兵 `__musicWantPlay`（bg-keep 让位守卫 + music-player 暴露）、`bgResumeFailAt`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；构建哨兵 85/85 + sw.js 2/2；npm run verify 10/10；verify-music-bg-resume 10/10（后台打断自动续播/用户暂停不续播/回前台恢复/来电 hold/元素重建）；verify-music-keep-coexist 10/10（音乐在播保活让位不抢、C6 playbackState=playing、音乐暂停保活收回、停止后保活媒体条恢复）；verify-ta-pause-live 15/15；真机：红米K80 Chrome 播放歌曲挂后台 15~60s+ → 歌曲媒体条持续显示不断、音乐不停；偶发外部打断恢复后歌曲条仍显示歌曲名而非「Mochi 后台保活」；用户暂停后才显示保活条 |
| 77 | 荣耀90 Edge：聊天里「我发语音」无法正常录音，录出来/播放是「滋啦滋啦」爆音（只有电流噪声听不清人声） | 根因（chat.js 录音）：pickVoiceMime 在安卓一律优先 `audio/mp4`(AAC)——标准安卓 Chromium（Chrome/Edge）的 mp4/AAC MediaRecorder 路径在荣耀 90 等机型上存在采样率不匹配内核缺陷（getUserMedia 输入 48k 与 AAC 44.1k 不匹配），录出爆音/电流声；而其原生默认 `audio/webm;codecs=opus` 路径稳定无爆音、Chromium 也能正常播放。另 `acquireVoiceStream` 全平台优先「关回声消除/降噪/自动增益 + 单声道」（vivo/iQOO 空录音的修复）在标准安卓浏览器上也会放大削波失真。修复：新增 `isAndroidWebView()`（UA 特征识别微信/雨见/vivo/OPPO/QQ/UC/百度/夸克/华为/小米/三星等内嵌壳）与 `voiceMimePreferOpus()`——标准安卓浏览器（Chrome/Edge 等非内嵌壳）录音格式 webm/opus 优先、麦克风约束 `{audio:true}`（AGC/降噪默认开）优先；iOS Safari（仅 mp4/aac 可录可播）与安卓 WebView（webm 能录不能播，vivo/iQOO 雨见等）保持原 mp4/aac + 关降噪单声道组合，零回归 | 哨兵 `voiceMimePreferOpus`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；构建哨兵 86/86；真机：荣耀90 Edge 录 1 句→试听/发送→语音清晰无爆音；iOS Safari 仍可录可播；vivo/iQOO 雨见等 WebView 仍能录能播（webm 不出声回归） |
| 78 | 此间（梦角世界）：不同联系人的梦角串桌——某联系人桌面下出现别的联系人的梦角 | 根因（cjian.js）：梦角归属靠名字反查 homeCidForName 太脆弱——①多个联系人 TA 昵称（lbl-partner）同名时 hits>1 认不到家，migrateSplit 把梦角一锅端给当前桌面，rehomeMisfiled 因"已在同名身份桌面"不纠正；②lbl-partner 与联系人注册名不一致时（用户设了 TA 昵称但梦角名是旧版用联系人名种的），homeCidForName 只匹配 taIdentity 认不到。修复：①给梦角加显式 cid 归属字段（seedIfEmpty/cjianManage 添加/migrateSplit 认亲时写入），后续归属按 cid 不再靠名字；②homeCidForName 同时匹配 taIdentity 与 contactName 双候选；③新增幂等 fixBelonging（启动+IDB 就绪后跑）：无 cid 的按名字认亲补 cid（认不到的固化在当前桌面），有 cid 但物理存错桌面的搬回 storeOf(cid)，同名冲突沿用幻影替换逻辑（外来者带互动痕迹而家里那位没有才替换） | 哨兵 `function fixBelonging` + `idn === n\|\|cn === n`（build.mjs FIX_SENTINELS 88/88）/ 验证：node --check 过；npm run verify 10/10；真机：多联系人下各桌面只显示自己的梦角，不再串桌；存量错放梦角启动后自动搬回 |
| 79 | 音乐·TA 暂停再播放互动时聊天里没有系统消息（只有字卡，不像收藏/切歌/换模式/预订/邀请那样有"XX …"系统消息） | 根因（music-player.js）：scheduleTaPauseIfLucky 触发暂停/恢复时只调 taPauseSendCard 发字卡（chatAddIn），漏调 chatAddSystem。修复：暂停处先 chatAddSystem(nm+' 暂停了音乐') 再发字卡，恢复处先 chatAddSystem(nm+' 又播放了音乐') 再发字卡（nm=partnerName()，与收藏/切歌等互动一致；先系统消息再字卡，参照 ta-ask.js 模式） | 哨兵 `暂停了音乐` + `又播放了音乐`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建后真机：播放歌曲等 TA 触发暂停→聊天出现"XX 暂停了音乐"系统消息 + 字卡；3.5s 后恢复→"XX 又播放了音乐"系统消息 + 字卡 |
| 80 | 红米 K80 Chrome：弱网/断网时播放网易云歌曲，诊断报「Cannot read properties of null (reading 'play')」未处理 rejection + 一堆 meting「资源加载失败」 | 根因（music-player.js）：startPlayback 等 5 处「play() 被拒 → muted 静音解锁」链路里，`audio.play()` 的 rejection 是**异步回调**，回调执行时全局 `audio` 可能已被 teardownAudio 置空——断网/弱网时 meting URL 加载失败 → audio.onerror → retryWithHttpsUrl **先 teardownAudio（audio=null）再异步拉直链**（8s 窗口），期间旧 play() 的 catch 回调触发 `const p2 = audio.play()` → 在 null 上读 'play' → TypeError → unhandledrejection（bundle 行号随构建漂移，崩溃源始终是这 5 处 catch 回调里的二次 play）。修复：5 处异步 catch 回调（startPlayback / tryResumePlayback / toggle / musicHoldForCall 恢复 / TA 暂停恢复）进回调先 `if (!audio) return;` 判空短路——换源回调/后台补播/手势兜底自会接管，不再对 null 调 play | 哨兵 `Cannot read properties of null (reading 'play')`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；构建哨兵 90/90；真机复现路径（红米K80 Chrome 断网/开飞行模式时切歌或让歌曲起播）→ 不再弹该 TypeError、音乐照常走换源/补播，诊断「最近错误」不再新增该崩溃 |
