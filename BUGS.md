# BUGS.md — Bug 修复规则（防回归、防反复修）

> 配套约定：与 `AGENTS.md` / `WORKLOG.md` / `FIX-REGRESSION.md` 协作。改本文件或 FIX-REGRESSION.md 时在 WORKLOG 留一行说明。

1. **修复前必查** **`BUGS.md`**：如果症状已登记为「已修复」，按台账记录的根因和位置处理，禁止当作新 bug 重新修一遍。
2. **修根因不修症状**：禁止 try-catch 吞错、禁止用重试/延时/遮罩层掩盖问题。
3. **最小改动**：只改与该 bug 直接相关的行。禁止「顺手优化」「重构整个函数」——重写会把别人已修复的代码还原，这是回归的最大来源。
4. **修复处加标记注释**：格式 `// FIX 2026-09-01 #编号 简述`。后续 AI 见到此注释，不得删除该注释、不得重构标记处的逻辑；确需改动时先在 `BUGS.md` 查该编号的根因。
5. **产物必须同步**：修完源文件必须跑 `node build.mjs` 重新组装 + 跑哨兵检查与 `npm run verify` 校验，产物过期 = bug 视为未修复（`src/` 是唯一修改入口，禁止直接改 `index.html` 来「修」）。
6. **修完四件事（v3.27.x 升级）**：
   ① 改完 src 当场跑 `node build.mjs --check-sentinels`（只检查不构建）——确认自己没把别人的修复锚点删掉，验证从「构建者收口时」提前到「每次保存后」；
   ② 登记 `BUGS.md`（含根因 + 验证方法 + 实机/无头验证到哪一步）；
   ③ 按台账「验证方法」自测该 bug 场景；
   ④ 抽查相邻功能没被改坏；复发 ≥2 次的修复必须补 `tools/verify-xxx.mjs` 行为断言并纳入 `npm run verify:all`（哨兵只证代码在，脚本才证行为对）。
7. **同一 bug 第三次复发**：停止盲修，在 `BUGS.md` 把它标记为「需人工分析」，写清已尝试过的所有方案和现象，交给用户决定。

