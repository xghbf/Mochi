# AI-RULES.md — AI 回答与做事规范（必须执行）

## 回答方式

1. 回答不超过 3 句话，只给结论，不解释推理过程。
2. 直接给完整结果，禁止「先列大纲→等确认→再展开」的流程。
3. 单次输出过长时分批给，不要一次性超长输出。
4. 代码改动只说三件事：改了哪个文件、加了什么、怎么验证，其余不写。

## Token 节约（AI 工作方式 · 必须执行）

1. **禁止通读** **`index.html`**：它是组装产物（约 210KB，读一次 ≈ 6 万 token）。改代码只读 `src/` 下的拆分源文件；需要定位时先 grep 行号，再只读相关片段。
2. **精准修改**：用局部替换改几行就是几行，禁止为小改动重写/重输出整个文件；回答里不贴大段代码。
3. **按需读取**：只读与当前任务直接相关的文件；同一会话内已读过的文件不重复读。
4. **日志限长**：协作日志每条 ≤100 字，只写：做了什么、改了哪些文件、版本号、自测结果。
5. **日志归档**：协作日志超过 15 条时，旧条目移入归档文件（平时不读它）。
6. **会话纪律**：话题切换即开新会话；一个会话只干一件事；干完立即结束，不追问不铺垫；会话过长时先总结进度存入日志再继续。
7. **派活一次说全**：给 AI 派活时，背景、要求、验收标准一次写清，避免多轮追问反复重发上下文。

