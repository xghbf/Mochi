# FIX-REGRESSION — 修复点回归清单

用户反馈过的关键问题 → 对应验证方式。**每次构建/上传前对照本清单跑相关检查**，
防止「已修复的问题在新版本复发」（历史教训：修复被并行会话覆盖 / 编辑器旧缓冲
回写 / 新文件漏接入 build.mjs，构建照常通过但功能已丢）。

> ⚠️ **禁止通读本文件**（当前仅保留最近 42 条，历史在 `FIX-REGRESSION-archive.md`）：构建/自查时先 grep 编号或功能关键词，再读命中行；全绿才提交推送。
## 使用方法

1. 构建后先看 build.mjs 的「关键修复哨兵」输出（自动检查产物特征，缺失会醒目警告）。
2. 按本清单跑相关专项脚本（`node tools/verify-xxx.mjs`）。
3. 全绿才提交推送；有红项先定位再上线。
4. **真机验证状态约定（v3.27.x）**：主清单验证方式里的【真机:待验证】= 只有无头验证，用户真机反馈正常后改为【真机:已确认(机型+日期)】。修 B 碰到相关文件时：【已确认】的条目必须复测防回归；【待验证】的条目至少不破坏其修复代码（跑哨兵）。


## 设备索引（修 B 前先查：我要改的文件压过哪些机型的修复？）

> 改任何文件前，按下方「涉及文件」列 grep 本表；命中机型 = 你可能压到它的历史修复，修完按对应条目的验证方式复测。
> 生成方式：脚本扫描主清单症状文本（v3.27.x）；新条目登记时请同步维护本表。

| 设备/机型 | 相关条目 |
|---|---|
| 小米 14U | 82、86、88、90、103 |
| OPPO Find X9 | 103、124 |
| iPhone 12 | 85、114 |
| iPhone 16 | 84、100 |
| vivo X200s | 104、117 |
| vivo Y35 | 96、100 |
| 苹果17 | 109、110 |
| 荣耀 x30i | 96、100 |
| iPad Air | 121 |
| iPad Safari | 121 |
| iPadOS | 121 |
| iPhone 12PM | 114 |
| iPhone 13 | 120 |
| iPhone 15 | 91 |
| iPhone13 | 112 |
| iPhone14 | 112 |
| iQOO Neo10 | 107 |
| 华为 Mate 4 | 108 |
| 华为 P50E | 92 |
| 小米15 | 106 |
| 小米15Pro | 118、131 |
| 红米 K60 | 115 |
| 红米 K80 | 93 |
| 红米 弱网或断网刷新 | 87 |
| 苹果16 | 111 |
| 荣耀x60i | 98 |

## 清单

| # | 用户反馈问题 | 修复要点 | 验证方式 |
| --- | --- | --- | --- |
| 81 | 贴贴邀请：联系人发「申请贴贴」互动卡片，我同意后联系人回应的消息也显示「主动联系」小爱心标识（不应有） | 根因（chat.js）：openInvitePanelFor 的 cuddle 分支同意后调 `addIn(name+' '+pick(CUDDLE_REPLIES), { initiative: true })`——`initiative:true` 在 renderMsg（chat.js:2017）触发 `msg-hi-heart` 小爱心（本意标记 TA 主动发起联系）。但贴贴回应是「我同意后触发的回应」非 TA 主动发起，不该带爱心。邀请消息本身（"想和你贴贴"）带爱心合理（TA 主动找你），仅同意后回应误带。修复：cuddle 回应去 `initiative:true` 改传 `{}` | 哨兵 `pick(CUDDLE_REPLIES), { initiative: true })`（build.mjs FIX_SENTINELS，**absent**）/ 验证：node --check 过；待构建后真机：TA 发贴贴邀请→我同意→TA 回应不再显示小爱心；TA 主动发普通消息仍显示爱心（语义不变）；邀请消息本身仍显示爱心 |
| 82 | 小米 14U Edge：导出数据进度条走完、点了「确定」下载，浏览器不弹下载也没下载完；且导出后数据像是被清空（设置/联系人等本地存储全没，聊天/音乐等 IndexedDB 大键还在） | 根因一（data-backup.js anchorDownload）：blob URL 在 `setTimeout(1000ms)` 后被 `URL.revokeObjectURL` 作废——大备份（音乐/头像/聊天全量 base64 后轻松上百 MB）下载刚由浏览器接管、blob 还没读完 URL 就失效 → 无下载通知、无文件落盘（诊断实测该用户自动备份快照高达 172.7MB）。根因二（旧线上版本）：导出把整个 JSON **无条件**写进 IDB 自动备份副本键，大备份写副本成内存峰值主因 + 旧 idbSet 固定 4s 超时连败弹「存储异常」，导出过程被拖到崩溃边缘；崩溃/内存压力下安卓 Chromium 本地存储(LS)损坏 → LS 全空且后续写入全报 QuotaExceededError（该用户诊断：localStorage 0 键 + LS 写探针失败，IndexedDB 187.5MB 完好——数据其实没丢，全在 IDB）。修复：①anchorDownload 改长命 blob URL（pagehide 才释放 + 5 分钟兜底，anchor 不再 1s 移除）让大下载真正完成；②副本写入 >3MB 跳过（snapshotWritten 记录），且弹窗/toast 对未写副本的大备份如实提示「下载文件是唯一新备份」，不再谎称「已自动存入本机缓存」 | 哨兵改锚 `addEventListener('pagehide', function h()`（anchorDownload 长命 blob URL 特征；原 `const snapshotWritten` needle 已随副本机制下线删除，见 #86）/ 验证：node --check 过；构建哨兵全绿；真机（小米 14U Edge）：导出大备份→进度条走完→点「确定」→状态栏出现下载/文件正常保存；数据导出前后本地数据无任何变化（导出不写不删任何业务键）；导出弹窗提示「这个文件是唯一备份」 |
| 83 | 开屏「点击进入」在数据没加载完时就能点：浏览器 tab 还在转/loading，但开屏进入按钮已可点，进去后字卡/图片/聊天记录等数据不全（首次访问和老用户刷新都遇到） | 根因（idb.js）：`idbRestore` 的 12 秒保险丝在 IndexedDB 回填超时（低端机大量图片/字卡键）时调 `sendReady()` 强制设 `window.__mochiDataReady=true` **静默放行开屏**，但后台回填循环还在跑、未完成 → 开屏 `ready()` 返回 true → 「点击进入」可点 → 用户进入后读到的是空/部分数据。注释原承认这是「卡死 vs 数据暂时不全」的权衡，但用户反馈「进去数据都没加载完」不可接受。修复：①idb.js 保险丝不再调 `sendReady()`，改为派发 `mochi-restore-slow` 事件 + 设 `__mochiDataSlow`，**不**设 `__mochiDataReady`——只有 `finish()`（真全部回填完/空数据/读 keys 失败）才设就绪标志；恢复循环继续后台补齐。②clock.js「点击进入」按钮仍只认 `__mochiDataReady`（真就绪），收到 `mochi-restore-slow` 或 20 秒兜底且未就绪时显示「仍要进入」小链接（弱化样式，非推荐入口），点击后 hide 开屏 + openModal 提示「数据仍在加载，部分内容可能暂时看不见，建议稍后刷新」；原 20 秒自动 `hide()` 兜底删除（不再静默放行）。③loading 文案在 slow 时改「数据较多，仍在加载…」让用户知道在加载而非卡死 | 哨兵 `mochi-restore-slow`（idb.js）+ `splash-force-enter`（clock.js）（build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建后真机：①数据几秒内就绪→按钮可点→进入数据全；②数据超 12 秒未就绪→按钮置灰+「数据较多，仍在加载…」+「仍要进入」小链接出现；③点「仍要进入」→进入+提示数据可能不全；④IDB 真挂起→按钮永不放行，只能走「仍要进入」逃生口 |
| 84 | iPhone 16 Pro Safari（iOS 18.7，PWA standalone）：一直弹「存储异常：近期数据多次写入失败」弹窗，每会话必现；诊断里 IndexedDB 大键明细/开关持久化体检停在「读取中…」（IDB 读事务也慢/挂） | 根因（idb.js）：iOS 挂后台会杀 IndexedDB 服务进程，回前台后旧连接上事务失败。v3.25.x 的 `connLost` 只匹配 `InvalidStateError`，但 iOS 18 实测多报 `UnknownError`/`InternalError`/`TransactionInactiveError`（有时只挂起不报错）→ 连接永不重建 → 本会话后续写入全部失败 → 连续 5 次触发 `_idbFailNotify`「存储异常」弹窗（`_idbFailAlerted` 会话级，故每会话必现一次）。该用户诊断 9 条前后台切换（5 分钟内来回切 9 次），每次回前台都撞死连接。修复（idb.js）：①`connLost` 加宽匹配 iOS 连接级错误名（UnknownError/InternalError/TransactionInactiveError）；②新增 `armFgIdbReset()`（仅 iOS 启用）：`visibilitychange→visible`/`focus` 时主动作废旧连接引用并预拉起新连接，回前台后的首次写入不再撞死连接；③`idbSet`/`idbSetAll`/`idbDelete`/`idbClear` 补 `tx.onabort` 处理 + 所有事务错误都过 `connLost` 判死重建 | 哨兵 `armFgIdbReset`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；构建哨兵全绿；npm run verify 10/10；真机（iPhone 16 Pro Safari PWA）：进设置→看存储→回桌面挂后台→切回来再发消息/改设置→不再弹「存储异常」；诊断里 IndexedDB 明细不再长时间「读取中…」 |
| 85 | Safari 导出数据→Chrome 导入后丢失朋友圈内容数据和聊天记录数据（iPhone 12 Pro Max Safari 导出，Chrome 导入；诊断 v3.26.363：IndexedDB 只有 chat-msgs default=854B+cmtejn1u47jn=260B 极少，无 feed-posts；LS 有 feed-posts-snap=1.2KB 剥图快照） | 根因（data-backup.js + idb.js）：①导出 doExport `if (k in data.ls) continue` 让 chat-msgs 的 LS 有损小快照（≤20KB，liteSnapArray 剥图/截断）跳过 IDB 权威读取→导出有损快照丢图片/语音；②feed-posts >200KB 时 xyStore 只写 IDB 删 LS，Safari IDB 事务挂起/超时（idbGet 4s+4s）→主键彻底丢失只剩 LS 剥图快照；③导出 IDB 失败不读 memoryCache（idbRestore 回填值）无兜底；④导入 doImportGo 无条件跳过 chat-msgs 的 LS 写入，data.idb 无权威值时 chat-msgs 既不写 LS 也不进 IDB→彻底丢失。修复：①idb.js 新增 idbGetCached 暴露 memoryCache 读取；②导出权威键（chat-msgs/feed-posts）强制读 IDB 权威不跳过，IDB 失败→memoryCache 兜底→lsBig→LS，降级记入 exportMissing 导出结束在覆盖清单警告「以下数据可能未完整导出，请勿清空原设备」；③导入 chat-msgs 仅当 data.idb 有权威值才跳过 LS，否则把 LS 有损快照写进 IDB 兜底 | 哨兵 `idbGetCached`（idb.js）+ `isAuthorityKey`（data-backup.js）（build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建后真机：Safari 发带图聊天+带图朋友圈→导出（无降级警告）→Chrome 导入→聊天记录和朋友圈完整含图；Safari IDB 挂起导出降级→弹窗显示「⚠ 聊天记录/朋友圈可能未完整导出」→用户知情不清原设备 |
| 86 | 「自动备份快照」占掉近一半存储且永远不会更新（用户全部数据的一份完整副本；实测 743.6MB / 小米 14U 172.7MB）。存储页还写着「下次你再导出数据时会自动重建一份」——v3.27/v3.28 加了 ≤3MB 阈值后，大数据量用户的导出根本不写副本，这句话对最需要它的用户是错的 | 经用户确认整体下线。①**写入端**（data-backup.js doExport）：删除副本写入（连 `snapshotWritten = json.length <= 3MB` 分支与「正在写入自动备份副本」进度步骤一起去掉），导出提示统一如实说明「这个文件是唯一备份，本机不再另存副本」；②**消费端**：删 `checkLostAndOfferRestore`（启动「检测到数据可能丢失→从副本恢复」弹窗）与 garden.js `offerSnapshotRecover`（**跨域改动**，理由见 WORKLOG）——后者会对整包副本 `JSON.parse`，数百 MB 遗留快照等于一次 OOM/长任务；③**清理端**：新增 `purgeLegacySnapshot()`，数据就绪后 1.5s 删掉 LS+IDB 两处遗留副本键（只删这一个键，业务数据不动，静默无提示），用户不必再进设置手动点删除；④**UI**：查看存储页去掉「自动备份快照」整卡（#st-snap / #st-clear-snap / #st-snap-hint）、`catOf` 分类、`.snap` 与 `.storage-clear.danger` 样式；**保留**导出/导入与 `idbRestore` 对该键的排除（清理落库前仍可能读到，防自包含无限增长与误回填）、device.js 诊断大键明细（副本若因 IDB 故障没清掉仍能在诊断里看到）。已知代价（用户已确认接受）：不再有「业务键几乎全空时从本机副本自动恢复」的保险，数据丢失只能靠用户自己保存的导出文件 → notice.json「关于数据丢失」补写「本机不保留自动副本，务必自己存好导出文件」 | 哨兵：`purgeLegacySnapshot`（存在）+ absent `idbSet(SNAPSHOT_KEY`（导出再写副本即报警，同时守住 #73/#82 根因）+ absent `offerSnapshotRecover`；旧 needle `st-clear-snap`/`json.length <= 3 \* 1024 \* 1024`/`const snapshotWritten` 已删除或改锚（#42/#73/#82 行已标注被本行取代或改锚） / 验证：node --check 全过；`node tools/verify-garden-dataloss.mjs`（Case B 已改写为「播种遗留副本 → 断言启动后 IDB+LS 均被自动清理、期间不再出现任何副本找回弹窗、dummy 业务键完好」）；真机：更新后启动一次 → 设置→查看存储，IndexedDB 占用应下降约一半 → 再导出一次，弹窗提示「这个文件是唯一备份」；诊断「IndexedDB 大键明细」不再出现 `__auto-backup-snapshot`。**同日两处补强（各配一条新哨兵，且都用反向对照实测过）**：① 清理触发原来只有事件路径，而 #83 之后 12 秒保险丝不再设 `__mochiDataReady`（只派发 `mochi-restore-slow`）→ IDB 整轮挂起的设备上 `mochi-restore-done` 永不到达、清理一次都不跑，而 IDB 最慢、遗留副本最大的恰好是同一批机型；补 `purgeOnce()` 幂等包装（与 #90 的「删→复核→重试」链共用，保证只起一套）+ 20 秒墙钟兜底（照 idb.js `wrjMergeFromIdb` 的挂起兜底同款做法，清理只碰副本键、与 restore 完成与否无关）。② `idb.js` 的 LS→IDB 大键迁移未排除该键 → 以 LS 形态存在的副本（远古版本或手工改过的备份包）必然远超 `LS_BIG_LIMIT` 而被收进 `bigKeys`，整包读进内存 + 写回 IDB + 常驻 `memoryCache`，等于把刚清掉的副本复活一份还白钉几百 MB 堆；补 `continue` 排除，交给 purge。反向对照：撤 ① → Case E 复现「35 秒内副本一直在」；撤 ② → Case D 实测 `window.idbGetCached` 命中 420045 字符。新哨兵 `function purgeOnce()`（data-backup.js）+ `if (k === 'xy-home-v2:__auto-backup-snapshot') continue;`（idb.js）；`tools/verify-garden-dataloss.mjs` 新增 Case D（LS-only 超大副本 → 断言不迁移不常驻内存）/ Case E（注入「restore 永不完成」→ 断言 20 秒兜底仍清理），全套 27/27。**⚠ 提交陷阱备忘**：负向对照时把修复行注释掉，若该形态被 staged blob 残留提交，`minifyJs` 会整行丢弃 `//` 注释 → 产物缺 needle → 哨兵会醒目报警而不会静默上线；本次线上产物正确，但已提交源码 `src/js/idb.js` 一度残留该注释（工作区已修正，待构建者随下次提交收口） |
| 87 | iOS/安卓弱网（GitHub Pages 国内慢网）进入应用一段时间后「退回之前更新的旧版本然后白屏」，且退回的旧版桌面功能图标显示不全/少很多（iPhone Edge/iOS 18.7 反馈；诊断确认设备运行最新版 ts=1788067851963、SW 当前生效、.phone=812px 布局修复 78a2835 已生效） | 根因（src/pwa/sw.js）：SW 两处挑旧缓存都不看版本——①fetch 导航回退 `keys.reduce` 按 caches.keys() 原顺序探测，命中第一个含 index.html 的缓存（顺序不定、通常最旧优先），当前 CACHE 缺 index.html（慢网 precache 超时失败）时用户被送回**最旧**旧版 → 旧版布局 bug（图标缺失）+ 白屏；②activate 兜底 `hits.find(Boolean)` 保留的也是第一个（最旧）旧缓存，下次导航继续退回最旧版。修复：①新增 cacheVersion() 解析 CACHE 名 `mochi-<构建时间戳 base36>`（build.mjs 每次构建替换、单调递增）得版本号，解析不了按 0；②fetch 导航回退 keys 先按版本**降序**排序再 reduce 探测，命中「最新」旧 index.html；③activate 兜底 keep 取版本降序第一个（最新）旧缓存，其余照删 | 哨兵 `sort((a, b) => cacheVersion(b) - cacheVersion(a))` + `cacheVersion`（build.mjs sw.js 专项哨兵 3/3）/ 验证：node --check 过；构建哨兵 96/96；npm run verify 10/10；真机（iPhone/红米 弱网或断网刷新）：不再退回旧版布局/白屏，回退命中最新缓存；升级后首次弱网打开不再出现图标缺失 |
| 88 | 小米 14U Edge（Android/Edge 151，PWA standalone，诊断 v3.26.363 ts=1788074500839）两个症状同源：①「聊天记录会自己消失，差不多几个小时就消失不显示」；②「后台通知有时候会自己关闭」 | 诊断证据：**本项目 localStorage 键=0 个**、写探针 `QuotaExceededError`，而 IndexedDB 184MB 完好、`storage.persisted=true`、563KB 聊天记录在 IDB 里 → LS 已整库写不进（GitHub Pages 同账号各项目共用一个 origin 的 LS 配额，路径不隔离，最可能是同域其他站点占满）。根因＝**「启动时同步读 localStorage」而 LS 已废、IDB 回填是异步的**：①`active-contact` 读不到 → 每次冷启动静默切回 `default` 桌面 → 用户看到「记录消失」（切回原桌面即可见）；②`bg-keepalive`/`bg-notify` 全局键启动读空 → 按默认值渲染成「关」（且旧逻辑会把读空当事实写回覆盖）；③真数据丢失风险：权威读超时后 `saveMsgs` 仍把内存数组整包写进 IDB，LS 快照在废机上救不回来 → 一条新消息即可抹掉全部历史。修复：①**contacts.js** 读 `active-contact` 改走 `xyStore`（内存缓存优先，接上 #40 小键写日志回放），并新增 `correctCidFromIdb()`——回填完成 / `mochi-wrj-heal` / 16s 兜底时按 IDB 权威值自动切回原桌面（用户手动切过桌面则不干预，联系人须存在才切；**且只在开屏未消失或当前就停在手机主页时才切**，`setActiveContact` 会强制回主页，用户正在聊天/设置里时本次放弃、留给下次冷启动）；**同一批还要收口 `migrateLegacy.finish()`：原判空用裸 `localStorage.getItem(active-contact)`，LS 失效机上恒真 → 每次启动把回填好的权威值连同内存缓存/写日志一起改回 `default`，等于把 `correctCidFromIdb` 抵消，现改为 `regStore().get` 判空**；②**bg-keep.js** 新增 `reheatBgSwitches()`，回填完成后重读并重新应用两个开关，`keepUserTouched`/`notifyUserTouched` 保证手动操作不被覆盖；③**chat.js**（**跨域**，见 WORKLOG）`saveMsgs`/`saveMsgsNow` 加 `authOk` 闸门——本会话未读到该桌面权威值时只暂存 `pendingLocal`+写 LS 有损快照+`scheduleIdbRetry`，不再整包覆盖 IDB，15s 保险丝也改为补挂重试而非放行覆盖（宁可晚存几条，绝不覆盖全部）；④**device.js** 诊断单次遍历补报**整域** LS 键数/占用/非本项目最大键 + `localStorage 状态：…` 结论行（区分「同域其他站占满配额」与「本库损坏」），并新增 `__lsStatus` 自检：LS 写不进但 IDB 回填已完成时当场提示一次「本机浏览器本地存储受限，设置与记录已改用数据库存储，数据不会丢」（sessionStorage 去重；**提示走 device.js 自带的 `#cc-toast` 渲染，不要改成 `window.toast(...)`——build.mjs 把每个 js 文件单独包进 IIFE，chat.js 顶层的 `function toast` 挂不上 window，全项目 `window.toast` 赋值为 0，调它等于静默不显示**；且须等开屏 `.hide` 后再弹，否则被 z-index 999 的 splash 盖住） | 哨兵 `correctCidFromIdb` + `if (!regStore().get('active-contact'))`（contacts.js）+ `reheatBgSwitches`（bg-keep.js）+ `const authOk = chatDbReady && authLoadedPrefix === window.activePrefix();`（chat.js）+ `localStorage 整域=`/`本机浏览器本地存储受限`（device.js，后者原 needle `__lsStatus` 两版都在、测不出提示丢失，已改锚用户可见文案）共 6 条（build.mjs FIX_SENTINELS）/ 验证：node --check 全过；待构建后真机（该用户）：①冷启动不再回到 default 桌面、聊天记录直接可见（约 1~2s 内自动校正）；②后台通知/保活开关不再自己变关；③诊断出现「localStorage 整域=… 非本项目… 键」可定性配额被谁占；④LS 废机启动后看到一次「已改用数据库存储」提示（该提示链路已用无头 Chromium 实测 6/6：写探针抛 `QuotaExceededError` → `__lsStatus=unwritable(...)` → splash 在场时不弹 → `.hide` 后 `#cc-toast.show` 可见、文案正确 → 2.6s 自动隐藏；同场实测页内 `typeof window.toast === 'undefined'`）。用户自救（本版本上线前）：设置→联系人切回常用桌面，563KB 历史即重新显示（下次冷启动仍会再隐藏）；建议给 mochi 换独立域名，避开同账号 Pages 共用 LS 配额 |
| 89 | 手机端（安卓 Chrome/Edge，红米/小米/三星）点击【发送】按钮后，手动按返回/点空白处收起输入法，收起那一刻明显卡顿 | 根因（mobile-adapt.js 安卓键盘适配）：收起动画期 `visualViewport.resize` 高频触发 `syncAndroidKb`，其每帧写 `.phone.style.height` 后立即调 `_aPinPan()`——`_aPinPan` 读 `_aVV.offsetTop`/`scrollY` **强制同步 reflow**；叠加 `_aSchedCe`→`_aRefreshCe` 对所有 `.ce-box` 做 `void offsetHeight` 强制 reflow。每帧 resize 都触发，收起动画 ~15 帧内 ~50 次强制 reflow，主线程拥堵=卡顿。修复：新增 `_aClosing` 收起态标志（focusout 且键盘开着时置 true，focusin/复原时清 false），收起动画期 `syncAndroidKb` 只写 height 跟随 vv、**不调 `_aPinPan`**（收起期 offsetTop 通常 ~0，平移无需逐帧归零，复原时 `_aPanComp` 统一兜底），`_aRefreshCe` 在 `_aClosing` 期跳过 ce-box reflow（收起期 ce-box 位置随 layout 自动更新合成层，无需 toggle transform） | 哨兵 `_aClosing`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建后真机（红米/小米 Chrome）：聊天页输入文字→点发送→手动收起键盘→收起流畅无卡顿；弹键盘/打字/半框（问问TA/占卜）输入仍正常、文字不飞；v3.27.x「收起时输入行不飞」仍成立（复原时 _aPanComp 兜底归零平移） |
| 90 | 与 #88 同机同报障的补刀（小米 14U Edge，LS 整库失效）：**记录被真覆盖**的通路仍在——「读库失败」被当成「这个桌面没历史」，随后一条消息就整包写回去；跨桌面追加（后台查岗/贴贴/求聊天通知回来时触发）是同款判定，一条通知即可抹掉那个桌面的全部记录（用户视角＝「几个小时自己消失」，且不需要用户动手）；导出时键清单读失败还会出一份提示「全部数据完整」的近空备份 | **① idb.js**：新增严格三态探测 `idbListKeys()` / `idbHasKey(key)`（数组·true/false·null=没读到；超时/挂起统一按失败并作废连接重开），`idbGetAllKeys` 改为其兼容包装（失败退化成 `[]`，约 20 处老调用方行为零变化）；`chat-meta` 排除出 #40 写日志（防 LS 回滚把过期账本补回来）。**② chat.js**（**跨域**）：三处「读到 undefined」的复核全改走 `idbHasKey`，**只有 `has === false` 才认无历史**，true/null 一律按读取失败重试；跨桌面追加「读到有值却解析失败」时**绝不整包写回**；新增条数账本 `<prefix>:chat-meta` + `chatLedgerGuard` 缩水守卫（基线≥300 且新条数不足一半 → IDB 与 LS 快照都不写，并暂存 `pendingLocal` + 限流强制 `loadMsgs(true)` 重读合并；用户主动清空/整包导入会归零或对齐账本；判定全同步、零额外开销）。**③ contacts.js**：写 `active-contact=default` 前先向 IDB 确认库里确实没有（否则保持未设置，交给启动校正按权威值切，不再自己抹掉校正结果）；校正抽 `applyCidCorrection` 并支持直读 IDB（回填迟到也能切回）；default 联系人名取 `lbl-partner` 改走 xyStore。**④ data-backup.js**：导出清单读不到一律中止并如实提示（不出具假完整备份）；导入后聊天核对区分「没读到」与「确实没有」。**⑤ device.js**：新增「桌面归属体检」（三层 active-contact 并列 + 各桌面条数账本 + LS 残留快照键名，全程不 parse 大键），大键明细不再把「清单读取失败」显示成「无大键候选」 | `node tools/verify-chat-overwrite.mjs`（30 项源码结构断言，含「不得再用 `idbGetAllKeys` 判定键不存在」反模式扫描）+ 哨兵 8 条（`#90`）/ 验证：node --check 五文件全过；真机（该用户）下次报障直接看诊断「桌面归属体检」两行——`active-contact：…IDB=` 定性切错桌面，`条数账本` 定性是否被覆盖；正常路径回归：发若干条消息→切桌面/重进→记录完整、账本条数随之增长（诊断里 `【当前】<cid>=N条`） |
| 91 | iPhone 15 Plus（Safari / Edge / Chrome 三浏览器一致）：点进「系统预设字卡」可以正常滑动，但点【返回】那一刻卡住，即便卡回去了整个页面也会变得非常卡 | 根因（headless 390×844 实测）：`default-cards.js` 把当前分类**整包铺进 DOM**——main 分类 4903 行 = 33221 个节点 / 4628 个 checkbox，全站节点 1.08 万 → 3.3~4.4 万；①返回时各页 `MutationObserver` 的选择器扫描被膨胀文档放大（≥50ms 长任务 [104,57]、点击到两帧 161.9ms）；②iOS 系浏览器全是 WebKit，`display:none` 会销毁数万渲染对象、再进时整棵重建，主线程直接堵死＝「返回卡住」；③返回后 33221 节点**常驻不释放**，之后每次切页都继续付税＝「整页持续卡」（二次进入仍要 524.6ms）。修复：**① default-cards.js** `mountCardView`（dc/fc/dk 三库共用）渲染改**真虚拟窗口**：数据拉平成 `flat`（`{header}` / `{c,cat}`）+ `Float64Array` 高度与前缀和 + 二分 `indexAt`，只渲染视口 ±0.8 屏（条目数下限 24），顶/底 `.cc-vspace` 占位块撑回全高（滚动范围 269786px 与旧版一致＝全量行仍可达），实测高度后按 `delta` 静默补正 scrollTop；滚动容器**动态判定**（`clipsContent` 启发 + capture 阶段 `scroll` 事件用 `e.target` 锁定，兼容 dc 页由 page 滚 / fc 列表自滚 / 窗口滚动三种形态）；rAF 合并 + containment 迟滞 + `hidden` 变化重排；跨 tab 搜索结果按真实分类 `rec.cat` 写开关（委托监听不变）。**② chatcard.js**（用户「一起修」）：`refreshLibCounts` 的 force 分支改走带缓存的 `pubGroupsRaw()`，返回字卡库不再把多 MB 公用库 `JSON.parse` 两遍。**③ chat-pages.css**：`.cc-vspace` 样式 + 把 v3.11.x「全量渲染对性能无影响」的旧结论标注为已被实测推翻（该结论正是本次踩坑依据） | 哨兵 4 条（`const V_PAD = 0.8` / `function clipsContent(` / `cc-vspace` / `countOf(pubGroupsRaw())`）+ `node tools/verify-preset-card-window.mjs`（20 项：DOM 有界／滚动范围保留／窗口随滚动推进／末条＝数据末条／可见区无占位空白／返回无长任务／二次进出不退化／单卡开关键／搜索收窄与恢复／功能字卡页同规则），新产物 **20/20**、旧产物（`git show HEAD` 对照）**12/20** 且失败项正是这四类形态。实测：进入后全站节点 10850→11284（旧 +22244）、列表子树 154~285（旧 33221）、返回两帧 26.6ms 无长任务（旧 161.9ms）、二次进入 43.8ms / 返回 48ms（旧 524.6 / 160.1ms）。**【真机:待验证】**：iPhone 15 Plus 三浏览器复测「进预设字卡→滑动→返回」不再卡住、之后切页流畅；功能字卡页/查岗页滚动与搜索同样复核 |
| 92 | 华为 P50E（Android/Edge 151，PWA standalone，部署站 GitHub Pages）：「自定义字卡」→「公用字卡 / 专属字卡」→【表情包】添加图片，刷新重进网站后刚加的图片消失、没有保存（诊断时回复字卡池 sticker=0/image=0、IDB 大键明细无 cc-groups 键＝写入确实没落地） | 根因链：①字卡库带上图片后整包 JSON 常跨过 idb.js 的 **200KB 大键阈值** → `xyStore.set` 跳过 localStorage 同步快照，**只走 IndexedDB 异步 fire-and-forget 写入**（重启唯一依赖它）；②`scheduleSave` 的 **120ms 防抖**期间或 IDB 事务尚未提交时刷新/切走/切桌面，新增图片无任何备份直接丢；③chatcard.js **没有任何离页冲刷**（chat.js/group-chat.js 都有 beforeunload/visibilitychange 兜底，独缺字卡库）；④`openCcPage`/`leaveCcPageReset` 直接 `clearTimeout(editSaveTimer)`——120ms 窗口内刚上传的内容被静默丢弃；⑤切桌面时 `setActiveContact` 只在 `__activeCid` 变更前冲刷聊天（chatFlushSave），字卡库 pending 的防抖定时器切走后会把 A 桌面数据写进 B 桌面键。修复：**① chatcard.js** 新增 `ccDirty` 脏标记 + `flushCcSave()`（挂起中变更立即落盘，无变更零开销），挂 `beforeunload`/`pagehide`/`visibilitychange(hidden)` 三个离页事件（与 chat.js flushSave 同款口径）；`openCcPage`/`leaveCcPageReset` 改为先 `flushCcSave()` 再切作用域，不再清定时器丢数据；暴露 `window.ccFlushSave`。**② contacts.js** `setActiveContact` 在 `__activeCid` 变更前补调 `window.ccFlushSave()`（必须在本行之前——ccFlushSave 内 curStore 动态读 activePrefix） | 哨兵 2 条（`function flushCcSave`（chatcard.js）+ `if (window.ccFlushSave) window.ccFlushSave()`（contacts.js），build.mjs FIX_SENTINELS）/ 验证：node --check 全过；构建哨兵 125/125、sw.js 3/3、verify 10/10、verify-chat-overwrite 30/30、verify-preset-card-window 20/20。【真机:待验证】（该用户 P50E）：公用/专享字卡【表情包】上传图片→立即刷新/重进→图片仍在；上传后立刻切桌面→切回原桌面图片仍在、B 桌面无串数据 |
| 93 | 红米 K80（Android/Chrome）：信箱里点联系人的信回了信，不显示内容；重新点这封信也看不到回信；寄出的信也不显示内容（用户补充「列表里就是空的，完全没有我写信的内容」；再补充「回信后只有等联系人回了我的回信后才显示，之前无法点击查看只有空白」） | **根因（代码推理定位）**：`mailDbReady=false`（切桌面后 idbGet 未返回/启动早期）→ `load()` 降级读剥图快照（content 空）→ `submitReply` 用该 list 设 myReply 后 `save` → `mailMergeFromIdb` 合并 IDB 完整版（content 有 + myReply 空）与快照版（content 空 + myReply 有）时，`mergeLists` 原实现按 `letterLen` 整体取更大一方 → content 或 myReply 丢失 → 点开空白；TA 回信后 partnerReply 落地使整版 letterLen 最大胜出才显示。**根本修复**：`mergeLists` 改字段级合并——content/myReply/partnerReply/read 各取更完整一方，不整体覆盖；`openLetter`/`openReply` 开头重新 `load().find(id)` 取最新完整数据（防 render list 的 l 来自剥图快照）。**防御性修复（已提交 f143621，保留）**：① submitReply 回信后补 `selectMailTab('in')`；② sendLetter 改 `showPage`+`selectMailTab` 后再 `render()`；③ openLetter tc-body 无 `.mail-paper` 重试注入；④ render() 列表项数不符重试 innerHTML | 哨兵 1 条（`selectMailTab('in')` 在 submitReply，build.mjs FIX_SENTINELS）/ 验证：node --check src/js/mail.js 过；待构建者 build 后跑 verify-mail-ios-reply.mjs + 真机（红米 K80 Chrome）：寄信→「寄出的信」tab 立即看到信件；回信→「收到的信」tab 立即看到来信带「已回信」标签；点开信件正文/回信内容正常显示；切桌面后回信再点开内容不丢 |
| 95 | 朋友圈动态里，联系人（或自己）发 1 张图、2 张图、多张图时，图片显示大小三档都不一样（单图又窄又高、双图最大、多图正常），列表看起来参差不齐 | 根因（chat-pages.css v3.5.94 引入的 `.feed-imgs` 九宫格）：基础规则是 `grid-template-columns:repeat(3,1fr)` + `img{width:100%;aspect-ratio:1/1;object-fit:cover}`，但后面三条按张数特判把它改掉了——`.feed-imgs:has(img:only-of-type){max-width:66%}`、`.feed-imgs img:only-of-type{aspect-ratio:auto}`、`.feed-imgs:has(img:nth-of-type(2):last-of-type){grid-template-columns:repeat(2,1fr);max-width:80%}`。390×844 实测正文宽 324px 时格宽＝单图 67.3px（且 aspect-ratio:auto 让格高随原图比例自由变高，竖图高 100.9px、高宽比 1.5）／双图 126.6px／多图 104px。修复：删除这三条特判，所有张数统一按多图九宫格（每格恒 1/3 正文宽、1:1 裁切、图少时右侧留白）。**已知代价（按用户要求统一为多图档尺寸）**：单图不再是「原比例大图」，竖图/长图在列表里被裁成正方形缩略图，点开仍看完整大图（图片点击放大链路不变） | 哨兵 2 条 absent（`feed-imgs:has(`、`feed-imgs img:only-of-type`，特判被加回即报警）/ 验证：`node tools/verify-feed-img-size.mjs`（390×844 直读 src/css，测 1/2/3/4/9 图：反向对照重现 67.3/126.6/104 不一致，当前源码 5 档全 104px、高宽比全 1.00，6/6）。待构建 + 真机：同一条动态发 1/2/3 张图，格宽一致；点图仍看大图；表情包混排进图片网格后不再撑高 |
| 96 | 不同手机浏览器播放网易云歌单链接的歌曲，总是显示「点击播放被浏览器拦截，请再点一下屏幕继续播放」 | 根因：`music-player.js` 的 `startPlayback`/`toggle` 在 `audio.play()` 被 reject 时**不接收错误对象、不区分错误类型**，把所有失败都当自动播放策略拦截处理。网易云歌曲 `audio.src` 是跨域 meting URL，部分浏览器在跨域 media 数据未就绪时 `play()` 返回非 `NotAllowedError` 的 reject（`NotSupportedError`/`AbortError` 等，实为源加载失败/跨域/混合内容/meting 服务不可达），旧代码一律走 muted 静音解锁 → 仍失败 → 弹「被浏览器拦截」，吞掉了本应走的拉完整版直链/内置旋律兜底路径，且误导用户以为是浏览器问题。修复：`play().catch((err) => …)` 接收错误对象，仅 `err.name === 'NotAllowedError'`（真自动播放策略）才走 muted 静音解锁 + `handlePlayReject`；其他错误走 `retryWithHttpsUrl`（拉 https 直链重播）→ `demoFallbackOrError`（内置旋律/坏链提示），不再弹「被浏览器拦截」。`toggle`（暂停后再播）同款区分。**同批三处补强**：① `resolveNeteaseDirectUrl` 只接受 `r.redirected \|\| /^audio\\//i.test(ct)` 的响应为直链——meting 对 VIP/失效歌返回 200 空 text/html，旧代码当直链原样回投重播坏 URL，一进就「被拦截」；② 已死代理 `corsproxy.io`（返回 401 强制 API key）从网易云歌单/详情/时长/fee 四个源列表移除（vivo Y35+Edge 诊断实证只刷「网络失败 401」）；③ 播放拒绝按错误类型给提示——源加载失败不再谎报「被浏览器拦截」，改弹「在线歌曲加载失败：可能为会员歌曲、链接失效，或网络无法访问音乐源」（荣耀 x30i Wi-Fi 屏蔽音乐源的场景即靠此文案如实告知） | 哨兵 4 条（`err.name !== 'NotAllowedError'`、`r.redirected \|\| /^audio\\//i.test(ct)`、`https://corsproxy.io/?url=` absent、`在线歌曲加载失败`，build.mjs FIX_SENTINELS）/ 验证：`node --check src/js/music-player.js` 过；`node tools/verify-music-vip-filter.mjs` 6/6（VIP 移除 + 无手势拒播不弹提示 + 手势拒播弹提示）；`node tools/verify-music-single-audio.mjs` 15/15（弱网换源不产生第二个播放器，Audio mock 的 reject 已改 `re.name='NotAllowedError'`、meting stub 带 302/audio 头匹配新校验）。真机：网易云歌曲点击播放——源正常直接出声；源失败走「正在获取完整版直链…」→ 兜底且不弹「被浏览器拦截」；本地歌曲/真自动播放场景仍走 muted 静音解锁不受影响 |
| 97 | 聊天里联系人主动发送的消息，左上角小爱心标识有莫名其妙的灰色阴影 | `chat-main.css` `.msg-hi-heart` 删除双层 `filter:drop-shadow`（白色发光 + 黑色投影 `rgba(0,0,0,.22)`）；浅色气泡上黑色投影即用户所见灰色阴影，深色主题无此元素覆盖，一处删除即可 | 哨兵 1 条 absent（`drop-shadow(0 1px 1px rgba(0,0,0,.22))`，build.mjs FIX_SENTINELS）/ 验证：构建后目视——联系人主动消息爱心为纯粉色、无任何投影/发光；浅色与深色主题各看一条 |
| 98 | 荣耀x60i/夸克浏览器：聊天里联系人有提问，但主页【提问记录】没显示 | 根因：① 	a-ask.js pushAsk 发 ask-card 只写 chat-msgs 不写 ta-ask.history，history 只在 openAskReply 回答后写 → 未回答的提问不进记录；② 单选题点选项直接调 chatAskReply（chat.js:1367）不经 openAskReply → 单选题回答从不写 history（即使回答了记录也空），默认题库 11 道单选题用户大概率命中。修复：pushAsk 发卡时生成 askTs 透传进 chat-msgs 记录 + 同步写 pending history；包装 window.chatAskReply 按 askTs 更新 pending→answered（覆盖文字题+单选题两条路径，排除 deskCk 查岗卡）；renderAskRecords 渲染 pending 显示"待回答"标签 | 哨兵 3 条（status: 'pending'/__taAskReplyWrapped/	c-li-pending，build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建 + 真机荣耀x60i/夸克：TA 提问后立即进提问记录（待回答），回答后更新为已回答（文字题+单选题）；deskCk 查岗不污染 |
| 99 | 桌面【音乐】→【联系人收藏歌曲】：我把音乐库里的歌删掉后，联系人收藏的记录也消失了（要求：删歌后收藏记录依旧保留） | 根因：`music-favs-ta` 只存歌曲 ID 数组，渲染用 `findTrack(id).filter(Boolean)`——歌曲一删，ID 找不到对应曲目，记录整体消失成永远无法显示也无法移除的僵尸项。修复：`music-favs-ta` 改存完整快照 `{id,name,artist,neteaseId,url,cover,duration,favAt}`（addTaFav 入库时写全量）；旧纯 id 数据渲染时从库内回补快照自愈回写（renderTaFavList 迁移）；已删歌曲用快照展示 + 置灰 +「已删除」小标签 + 副行提示（可还原歌：「· 点击重新加入并播放」；不可还原：「· 文件已不在，无法播放」）；点击已删可还原歌 → 按 neteaseId/原 url 重新加入音乐库并起播（restoreTaFavSong，新条目 id 为 sm_fav_* 并回写收藏条目指向新 id）；不可还原歌移除按钮仍可用 | 哨兵 2 条（`function taFavList()`、`ta-fav-gone`，build.mjs FIX_SENTINELS）/ 验证：`node tools/verify-music-ta-fav-keep.mjs` 10/10（旧纯 id 数据自愈渲染+回写、已删歌置灰+标签+提示、删库内歌后收藏保留+点击重新入库起播+条目指向新 id、不可还原歌可移除）。真机：删掉音乐库歌曲后进「联系人收藏歌曲」记录仍在（置灰带「已删除」），点它重新入库播放 |
| 100 | Mochi 诊断信息读取中截断三修 + device 体检（vivo Y35/荣耀 x30i/iPhone 16 Pro 多台真机诊断报告「读取中…」永远不补全，报障信息缺失） | ① `window.__jsErrors` 全项目无人初始化 → 功能文件启动异常被静默丢弃：新增 try 预初始化兜底；② 原 3s 单保险丝把 IDB 慢机的「最近错误/开关持久化体检/桌面归属体检/IDB 大键明细」整批截成裸「读取中…」：改软/硬双预算首屏标注（硬超时给「未完成（本机存储无响应…）」、软超时标「未读到（本机存储响应慢，稍后自动补全）」并后补）；③ 终态回填直写可见 #modal-textarea + 弹窗判活（ctl.text 的 setter 只写 hidden 的 #modal-input，回填曾静默失效）；④ 角标按最后一条错误时间戳判未读（原存条数，环形写满后永远算不出未读）；⑤ 最近错误环形上限 5→20 且调用栈只给最近 3 条（5 条窗口用户报障时早已刷掉；全带栈会把报障文本撑到剪贴板截断） | 哨兵 5 条（__jsErrors 预初始化/双预算标注/modalAlive 判活/SEEN_KEY 时间戳/ERR_CAP=20，build.mjs FIX_SENTINELS）；哑哨兵体检 `node tools/verify-sentinel-teeth.mjs`（逐条删 #100 修复行重建必报警且构建退出码=1）；诊断链路 18 项 `node tools/verify-diag-report.mjs`；一次性复跑全部回归脚本 `npm run verify:all`（= `node tools/verify-suite.mjs`，分 通过/断言失败/环境不满足/疑似撞车/超时 五类计数，并发跑批集中分配 `MOCHI_CDP_PORT` 防端口撞车，清单清干净后可加 `--strict` 当门禁）；runner 分类判据反向对照 `node tools/verify-suite-classify.mjs`（12 项，含「本机 ECONNREFUSED 必须判撞车而不是判需要外网」）；红项机械分类 `node tools/verify-triage.mjs --log tools/tmp-suite.log`（A 疑似漏接入 / B 期望过期 / C 运行时断言 / D 复跑转绿 / E 定位不到），其判据反向对照 `node tools/verify-triage-classify.mjs`（16 项，钉住「断言标签/测试输入回显/删除型反向断言/`join` 拼串/文件路径/helper 造元素」六类假期望过期） |
| 101 | 设置→【查看存储】看不出「大头是谁、哪个桌面占的、这些数能不能信」（用户反馈想看得更清晰）：明细是几十行流水账；展开区是 `xy-home-v2:cx1:chat-msgs` 这类机器键，用户读不出是谁的数据；总占用直接显示 `navigator.storage.estimate()` 的**整域**用量，把同域名下其他站点算进本应用头上（#88 同源）；IDB 清单读不到时退化成 `[]`，显示成「0 键」冒充「库里没有」 | ① 明细只列占用最大的 5 类 + 占比条（条长按**平方根**比例：实测某类占 94% 时线性条把第 3~6 名全压到 1.5% 下限、彼此分不出座次；最大项仍满格，精确份额看行末百分比数字），其余折成「其他 N 项合计」，点开逐类列名列大小（核对覆盖没有变难；聚合行**刻意不画条**——多类加总与单类不同口径，画了会看着比第 4、5 名更大）；② 展开区键名走 `deskNames()`+`labelKey()`，per-cid 命名空间换成「桌面名 · 键名」（`window.getContacts()` 的 cid→name），键数超出列出上限时如实标注「共 N 个键，仅列前 M 个」；③ 总占用拆双口径分行：「本项目占用合计」（本页面统计到的 LS+IDB）／·localStorage／·IndexedDB／「同域其他站点（非本应用）」／「浏览器整域已用 · 配额」，`lsStats` 顺带统计非本项目前缀键，页头补一句口径说明；④ IDB 键清单改走 #90 严格三态 `window.idbListKeys`（读不到＝null，不再 `idbGetAllKeys` 退化成 `[]`），失败时明细顶部红条告警 + IndexedDB 行与合计都标「未计入」；⑤ 文案口径修正：估算大小写清按字符数 ×2（UTF-16）不是字节数 | 哨兵 5 条（`function pctOf`／`function labelKey`／`window.idbListKeys \|\| window.idbGetAllKeys`／产物「本项目占用合计」／`css/setting.css` 的 `.storage-cat-bar`）；`node tools/verify-storage-clarity.mjs` 20/20（无头 Chrome 打**产物**：播种两桌面+多分类+25 键分类+同域非本项目键，验 Top5 与聚合行、条长平方根且递减、桌面名键名与截断标注、双口径自洽 self≈LS+IDB、monkey-patch `idbListKeys→null` 验告警链、全程无 JS 错误）；`npm run verify` 10/10。**反向对照**：把「其他合计」行也画条 → A3/A6 复现（聚合行比第 4、5 名条更长、座次反了）；needle 换回注释文字 → 新增的「针在注释里」哑哨兵体检立即报出 |
| 102 | 群聊里使用【群聊决定】【帮我决定】，答案却发到了【聊天】里（而不是正常发送到群聊） | 根因：两个决定面板（chat-decision-panel/chat-gdecision-panel）嵌套在 page-chat（单人聊天页）内；群聊更多面板点这两项时 group-chat.js 捕获监听一律切到聊天页，且 decision.js/group-decision.js 结果统一走 chatAddIn 发到聊天消息列表。修复：① template.html 把两个面板移到 .phone 级（.poke-card 为 absolute 相对 .phone，聊天页/群聊页共用，z-index:70 高于 .page）；② group-chat.js 群聊更多面板点 more-decide/more-gdecide 不再切聊天页；③ group-chat.js 新增 gcSendDecisionText（结果作为 special:'system' 系统消息写入群聊 msgs 并渲染，居中 msg-poke 样式、换行转 br，持久化刷新仍在）+ gcIsVisible（判 page-group-chat 可见）挂 window；④ decision.js/group-decision.js openPanel 记录 panelFromGroup=gcIsVisible()，发送时群聊上下文→gcSendDecisionText，否则 chatAddIn；聊天页打开两功能行为不变（仍发到聊天） | 哨兵 `gcSendDecisionText`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；待构建后真机：群聊更多→帮我决定/多人决定→出结果，答案应显示在群聊消息流；聊天页更多→帮我决定→结果仍发到聊天 |
| 103 | OPPO Find X9 Chrome：数据导出一导出就崩溃/导不出来（本地数据约 100MB：IDB chat-msgs 55.6MB + cc-groups 40MB + emoji-groups 15.7MB） | 根因（data-backup.js）：导出尾段 `JSON.stringify(data)` 把全部数据一把生成整个 JSON 字符串——大备份设备上「源数据 + 整包字符串 + stringify 内部缓冲」峰值内存近 2 倍文件体积，Chrome 安卓标签页 OOM 直接崩溃（同类：#73 iOS Safari 闪退 / #82 小米 14U Edge）。次要嫌疑：Blob→base64 先拼整块二进制串再 btoa（内存翻倍）+ `String.fromCharCode.apply(…,0x8000 个参数)` 栈溢出；`navigator.share({files})` 分享 100MB+ 文件复制进 intent。修复：① `jsonToBlobStreaming` 逐键序列化、每 ~4MB 合并进 Blob（Blob 拼接是引用不复制）、每序列化完一个大键立即 delete 释放、每 4 键 setTimeout(0) 让出主线程——峰值内存约砍半且不再有整包字符串；② `blobToBase64` 按 3 的倍数分块 btoa，去掉 apply 与巨型二进制临时串；③ `saveBackupFile` 分享面板加 50MB 上限，大备份统一走「确定后下载」；④ `exportCoverage` 超大键（>1MB）不再整包 JSON.parse 统计，只标「✓有（数据较大）」 | 哨兵 `jsonToBlobStreaming`（build.mjs FIX_SENTINELS）/ 验证：node --check 过；build 哨兵 156/156 + sw.js 3/3；npm run verify 10/10；tools/check-databackup.mjs 7/7（无头 Chrome 点击导出/导入无异常）；单元测试：jsonToBlobStreaming 输出与 JSON.stringify(data) 逐字节等价（含中文/emoji/引号/大 base64）+ blobToBase64 与标准 btoa 各长度一致。【真机:待验证】（OPPO Find X9）：导出大备份进度条正常走完→弹「备份已打包完成」→点确定能下载完整文件；导出前后本地数据无变化 |
| 104 | vivo X200s（V2458A）Android 16 + Edge 151：点【导出数据】一直停在「正在打包数据文件」，永远出不来文件。诊断实证：IDB 候选键合计约 806.8MB（default:chat-msgs 514.2MB、cc-groups-public 396.1MB、fav-msgs 28.3MB、15 个 music-file 各约 10MB），配额已用 970.9MB，LS 仅 1.9MB，未处理 promise 报 `RangeError: Invalid string length at doExport`（且「已是最新」——线上产物只到 #103 的逐键 stringify，未推） | 根因三层叠加（data-backup.js）：① #103 只做到「逐键 stringify」，单个大键仍要一次分配约 2.57 亿字符（chat-msgs 514MB 是 UTF-16 字节数），加上 800MB 全量对象图在读完前常驻 → 跨不过 V8 64 位单串上限 kMaxLength 约 5.37 亿字符 → RangeError；② 入口是裸调用 `doExport()`，异常变成未处理 promise rejection，`impHide()` 永不执行 → 遮罩冻在「正在打包」（＝用户看到的现象），错误只在诊断里看得见；③ 路由用的 `byteLen(非字符串)` 内部又整包 stringify 一遍只为量长度＝再复制一份大键。修复：①`createJsonPack` 加 `packValue` 加 `packString`：值内按 `PACK_DEPTH` 逐元素下钻、超 `PACK_SLICE`（1M 字符）的字符串分片转义（代理对不劈开）、每 ~4MB 合并进 Blob、每 `PACK_YIELD` 片段让出主线程，任何时刻内存里最多一个大键且单片段恒 ≤1M 字符；②`readNext()` 逐键「读→就地序列化→立即释放」，`own` 标志确保只有 IDB 新读出的私有副本会被改写（memoryCache、LS 共用值一字不动，防把业务数据写坏）；③入口改 `Promise.resolve().then(askExportMode).then(doExport).catch(...)` 且 `doExport` 内 try/catch → 遮罩必定收起，`reportExportError` 如实报出错环节、键名、体积，`string length` 类直接指路更小范围；④`overSmallLimit` 廉价浅判（超阈值早退，绝不为量长度 stringify）替代 byteLen；⑤`askExportMode` 本机用量超 150MB 才弹备份范围（完整、不含音乐文件、只备份文字、取消，`lock` 强制做选择），成品超 120MB 如实提示「新设备可能导不回来」，完成文案按模式措辞（精简备份不再谎称「全部数据完整」），导入侧读大文件按错误类型给文案（不再谎报「无效的数据文件」）；⑥覆盖清单改 `cover.see` 逐键累加（打包后数据已释放，不再二次遍历），同键以 IDB 权威值覆盖 LS 有损快照 | 哨兵 6 条（`createJsonPack`、`function overSmallLimit(v, limit)`、`reportExportError`、`askExportMode`、`这份备份太大，本机读不进去`，加 absent 守卫 `doExport();`＝裸调用回来看见即失败）。专项脚本 `tools/verify-jsonpack.mjs`（28 项断言，从源码切出打包器直接测）：与 `JSON.stringify` **逐字节等价**（中文、emoji、引号反斜杠换行控制字符、U+2028/9、3M 字符超长串分片、代理对正好落在分片边界、undefined 与函数属性省略、Date/Map/Set/TypedArray 不拍平、NaN 与 Infinity → null、深嵌套越界、整份备份文件总装），加内存有界断言（40M 字符数据全程单片段 ≤1M、own=true 写完即释放、own=false 源值零改动、overSmallLimit 零 stringify 调用）。npm run verify:all 会自动带上本脚本；check-databackup.mjs 覆盖导出导入入口点击无异常。【真机:待验证】（vivo X200s Edge 构建后）：点【导出数据】→ 弹出范围选择（选「不含音乐文件」与「只备份文字」各试一次）→ 进度条走完 → 弹「备份已打包完成」→ 点确定能落盘；导出前后本机数据零变化；选「完整备份」若仍失败必须弹窗说明卡在哪一步而不是冻住。已知边界：「只备份文字」只剥已解析成对象/数组的值里的 base64，旧版本把整段 JSON 当文本存的大键内 dataURL 仍会保留（这类键体积可能不如预期小，需要时先清理再导） |
| 105 | 钓鱼小游戏「今日收获」里【你】和【TA】两栏各有一个「留」复选框，但**只想留 TA 钓的那条做不到**——只要品种相同，我这侧的鱼也被一起当成「留」不卖（用户反馈：「我只想留他钓的鱼，但品种一样就必须把我的也一起留下，这个是 bug 吗」）。附带：勾完「留」后底部「可出售 N 件 · +¥」不变，要等下一次重绘才更新 | 根因（fishing.js）：`keep` 只按品种记一个开关（`keep[鱼id]`），两栏共用同一份、`sellAll` 在 `['mine','ta']` 循环里 `if (keep[id]) return;` 一次判定同时跳过两侧 → UI 给了两行独立复选框，状态层却没有归属维度。修复：① keep 键改带归属 `keepKey(side, id)`（`mine:<id>` / `ta:<id>`），渲染按行读 `keep[keepKey(side,id)]`、复选框带 `data-side`、事件按 `data-side+data-id` 写；② `loadToday` 内经 `normalizeToday` 把旧纯品种键（键不含 `:`；鱼 id 均为 `fish_xxx`/`sp_xxx`/`gift_xxx` 不含冒号，可安全判旧）一次性展开到两侧＝原「同品种两侧都不卖」语义，用户零感知，首次交互落盘后旧键自然被替换掉；③ 复选框 change 落盘后补 `render()`，合计即时同步；④ 出售落账后清掉该侧已无存货的残留「留」标记（否则同品种当天再钓到会被上次遗留的标记自动置留）。纪念品（`cat:'gift'`）本就不参与出售、不渲染复选框，行为不变 | 哨兵 5 条（`function keepKey(side, id)`／`if (keep[keepKey(side, id)]) return;`／`keep[keepKey('mine', k)] = 1`／`keepKey(cb.getAttribute('data-side')`／`if (!t[side] \|\| !t[side][id]) delete t.keep[k];`，均在 `js/fishing.js` 内唯一）。专项脚本 `node tools/verify-fishing-keep.mjs` **17/17**（静态 6 条含反向断言「旧 `if (keep[id]) return;` 已清除」；运行时用无头 Chrome 打自组装 src 页面真点复选框：只勾 TA 栏→存储只有 `ta:fish_small` 且我栏不受牵连、合计 5 件→2 件即时更新；出售只清我这侧，TA 留的 3 条与入账 400 分都精确；残留标记清理；旧纯品种键自愈展开且取消后不回灌；纪念品行无复选框）。运行时把 `window.setInterval` 打桩，防 TA 状态机随机加鱼让件数断言飘。**反向对照**：`sellAll` 判定退回 `keep[id]` → A1/A2 立即报红并中止（已实测）。npm run verify:all 会自动带上本脚本。已知边界（未做，需时再提）：同一侧同品种只记份数（`t.mine[id]=n`），「留」＝那一栏该品种整批留，做不到「5 条里留 2 条」；`cookMine` 烹饪取鱼不认「留」标记，会直接扣份数。【真机:待验证】：钓鱼→今日收获→只勾 TA 栏某品种→出售，我的同品种应被卖掉、TA 的保留 |
| 106 | 贪吃蛇**全屏**下（手机端进游戏即自动全屏）赢/输之后想再来一局：①整体格子偏大，得先把画面缩小才点得到【再来一局】；②【再来一局】按钮显示不完全（小米15 Pro + Chrome，视口 384×752） | 三处叠加：**A** 全屏结算从不重铺画布——`showResult` 只调 `refitNonFs`，而它 `if (…\|\| isFs) return` 在全屏下直接早退，地图原尺寸顶着结算块把按钮挤出屏（＝现象①）；**B** 高度预算漏算 `.snake-fs` 的 `gap:min(2vh,2vw)`，360/384/390/412 宽**空闲态**就已溢出 17～29px（＝现象②，与结算无关），同时删掉 `Math.max(240,·/260,·)` 虚报（极矮/横屏量到多少用多少）；**C** 量算误差（字号换行、gap 取整、字体渲染）无法归零 → `applyCell` 用 `scrollHeight - clientHeight` 实际溢出自查回退最多 4 轮，格子下限 9px；配套 `scrollAvail()` 统一两处重复量算、`refitAll()` 在结算/开始/空闲三处显隐后重铺、`renderBest()` 提前到 `toggleFs()` 之前（晚一行量不到最长纪录行）、CSS 兜底 `overflow:hidden auto`（横屏/极矮屏滚得到，canvas 有 `touch-action:none` 不影响滑动控制） | `tools/verify-snake-fs-result.mjs`（无头 Chrome：384×752 / 360×640 / 390×844 / 412×892 严格 + 752×384 横屏兜底 + 半框对照，逐视口断言 空闲/结算 按钮完整可见、`elementFromPoint` 命中按钮本体、画布非空白、结算地图 < 空闲地图、点按钮真开新局）。**反向对照已实测**：HEAD 旧源构建＝20/41（溢出 184～230px、`hit=offscreen`），修复后 41/41。哨兵 4 条（`js/snake-game.js` ×3 + `css/chat-pages.css` ×1） |
| 108 | 华为 Mate 40 Pro + Edge：清理会员歌曲（音乐日程卡片→自动清理）一直「无法自动清理会员歌曲，显示网络不可用」。诊断实证：`navigator.onLine=true`、存储/服务正常，仅两条 `https://proxy.cors.sh/…/api/song/detail…` 返回 **HTTP 520** | 不是用户断网，是「清理会员歌曲」依赖的**第三方 CORS 代理瞬时故障**（`proxy.cors.sh` 偶发 520 源站波动），而代码：① 排最前的 `api/v6/song/detail` 接口早已失效（实测返回 `{"code":404,"message":"接口未找到！"}`，每次白等 6s）；② 后备代理 `allorigins`/`codetabs`/`cors-anywhere` 现全部超时、`corsproxy.io` 需 API key——等于无可靠兜底。修复（music-player.js）：① 弃用失效 v6 接口，只用 legacy `api/song/detail/?ids=`（实测经 proxy.cors.sh 直达返回 fee=1/4）；② `fetchNeteaseFees` 重构为 `job/pr/retryLeft`：代理 `5xx/429` 视为瞬时抖动，`400ms` 后同代理自动重试 1 次再轮换；③ 失败文案由「网络不可用」改为「网易云查询服务暂不可用，请稍后重试」，不再误导用户以为断网。全程绝不把「查不到」当「免费」误删（维持原防误删语义） | 哨兵（build.mjs FIX_SENTINELS）：`r.status >= 500 \|\| r.status === 429) throw { retry: true, msg: 'HTTP ' + r.status }`（music-player.js 内唯一）+ 文案「网易云查询服务暂不可用」。`node --check` 过；待构建后构建哨兵全绿 + 真机（华为 Mate 40 Pro Edge）音乐日程卡片→清理会员歌曲：有会员歌时应能列出可移除清单；代理 520 时不应再冒「网络不可用」，应重试后出结果或如实提示「查询服务暂不可用」 |
| 107 | iQOO Neo10 + Chrome（Android 16 / 视口 360×740 / DPR 3.5）：群聊的气泡和字体**不管怎么换颜色都是纯黑的**（换色无效、非群聊功能代码问题） | 根因定位：group-chat.js 的 `applyGcBeauty` 用 CSS 变量（`--msg-in-bg/ink/out-*`）内联写在 `#page-group-chat` 上，机制本身在无头 Chrome（浅色/深色/换色）实测完全正常（换 `out-bg` 为粉 → 气泡即变粉）；且该现象只在**这台机型**出现＝设备/系统环境因素，而非通用代码回归。最可疑元凶＝**安卓 Chrome 强制深色（Auto Dark）**：本 app 深色由 `data-theme` 手动开关、不跟随系统，浅色态 `:root` 未声明 `color-scheme`；当手机系统处于深色时，Chrome Auto Dark 会在系统深色下**无视网页配色**把内容整体压成纯黑（与「换色无效、全部纯黑、仅此机」完全吻合，且无法在无头桌面复现）。修复：base.css `:root` 显式声明 `color-scheme:light`（主动告知浏览器「本页为浅色设计、深色由本应用手动管理」，退出 Chrome 强制深色覆盖）；`[data-theme="dark"]` 同步补 `color-scheme:dark`（优先级靠后覆盖 :root，保证应用自己切深色时原生控件仍随深色） | 哨兵 `color-scheme:light`（css/base.css，build.mjs FIX_SENTINELS）。验证：build 哑哨兵体检 0 条；npm run verify 10/10；临时无头复测——浅色 out=#111 底/白字、in=白底/#111 字，换 `out-bg` 粉→立即变粉，深色 data-theme=dark 下 out=#3a3a3a/白字、in=#2a2a2a/#f0f0f0 均正常。【真机:待验证】验收（iQOO Neo10）：① 若手机系统开深色、app 显示浅色 → 本版后群聊气泡/字体应恢复正常且能换色；② 若仍纯黑，请在群聊设置→美化里检查是否设置了**自定义气泡 CSS**（带 `!important` 会绕过颜色开关），清空即可 |
| 109 | 苹果17 自带浏览器 + **全屏模式**：不只聊天页，**其他所有功能页整体上移**，顶部内容/返回按钮被推出屏外点不到 | 根因（base.css）：`.phone` 高度在 iOS 全屏/浏览器态由 `mobile-adapt.js` 实测的 `--mochi-ios-h`（=visualViewport.height，缩放后）接管；**该值在个别 iOS 版本 / 全屏过渡 / 工具条显隐时机超过 `100dvh`**，而 `html,body` 是 `display:flex; align-items:center`——`.phone` 比视口高时 flex 居中把它顶部推到负值，整页上移（探针实测：h=900/950 在 844 视口下 `.phone.top` 为 -28/-53）。这是 `.phone` 级问题，**覆盖聊天页在内的所有功能页**。修复：两条 `--mochi-ios-h` 高度规则加 `min(var(--mochi-ios-h,100dvh), 100dvh)` 钳到 ≤视口（① 非 standalone 浏览器态 `html.ios-vv-fit:not(.ios-pwa-standalone) .phone`；② standalone+全屏 `.ios-pwa-standalone.ios-fs-active .phone`）。键盘期不受影响（该期摘除 `--mochi-ios-h` + 内联 height 接管） | 哨兵 2 条（build.mjs FIX_SENTINELS，needle 各带完整选择器保证唯一）：`html.tablet.ios-vv-fit:not(.ios-pwa-standalone) .phone { height:min(var(--mochi-ios-h, 100dvh), 100dvh)` / `html.tablet.ios-pwa-standalone.ios-fs-active .phone { height:min(var(--mochi-ios-h, 100dvh), 100dvh)`。探针 `tools/tmp-fs-shift-probe.mjs` 验证机制（uncapped 负 top → capped 归 0）。【真机:待验证】（苹果17 自带浏览器+全屏）：聊天 / 字卡库 / 日历 / 设置 等任意功能页顶部栏与返回按钮均应完整可见可点；若仍上移请反馈具体是哪个页面 |
| 110 | 苹果17 自带浏览器 + **全屏模式**：聊天页 `--mochi-ios-h`（=visualViewport.height）个别全屏过渡/工具条显隐时机**低于** `100dvh`，`#109` 加的 `min(...)` 取到偏小值 → `.phone` 被压矮，底部聊天输入栏**整体偏上、不贴合手机底部** | 它的孪生场景：`--mochi-ios-h` 不在「偏大」（#109 整页上移）而是「偏小」（本行输入栏上浮）。修复（mobile-adapt.js `syncVvFit`）：**全屏态一律摘除 `--mochi-ios-h`**、让 CSS 回退 `100dvh` 填满全屏——全屏下 `100dvh` 就是整块可视高，不需要也不该用实测值；判定类 `fs-active` / `fs-css-active` / `ios-fs-active` / `ios-native-fs` 任一命中即摘除返回。摘除后高度精确=`100dvh`（不超过视口，`#109` 的整页上移不会复发，两场景一并收敛）。键盘期逻辑不变（该期本就摘除） | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 mobile-adapt.js 内唯一）：`d.classList.contains('ios-fs-active') || d.classList.contains('ios-native-fs')`。【真机:待验证】（苹果17 自带浏览器+全屏）：进入聊天 → 底部输入栏应贴合手机底部；再退回桌面/进设置等页面 → 顶部栏与返回按钮仍完整可见可点（#109 不复现）。已有 #109 探针 `tools/tmp-fs-shift-probe.mjs` 理论仍适用（本修复后 `.phone` 不再用实测高、恒为 `100dvh`，top 恒 0） |
| 111 | 苹果16 添加到主屏幕 + **全屏模式**：桌面顶部「Mochi / 时间 / 电量」那一行不见了、被遮挡 | 根因（base.css）：iOS 全屏模式（`.ios-fs-active`）原设计隐藏应用内模拟状态栏（`display:none`）把内容顶到系统状态栏下方、避免与 iOS 系统栏重复——但桌面顶部那一行是全站品牌信息，隐藏后用户只见空顶。修复：全屏态**不再隐藏状态栏**（删 `.ios-fs-active .phone .statusbar { display:none }`），同时删掉同规则的 `.ios-fs-active .phone { padding-top:max(env-safe-area,12px) }`——状态栏自身已带 `env(safe-area-inset-top)` 安全区留白（见手机端 `.statusbar` 规则），两者叠加会双倍白带。`.phone` 高度相关规则（#109/#110 那几条 `min(...)`/`100dvh`）不动 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 base.css 内唯一）：`改为全屏时也保留状态栏。状态栏自身已带`。验收：构建哨兵全绿；无头探针 `tools/tmp-wp.mjs` 强制 `ios-pwa-standalone ios-vv-fit ios-fs-active` + 注入壁纸——`.statusbar` 由 `display:none` → `display:flex`（rect 高 33px），`.phone` padding-top 为 0；桌面首页顶部品牌行可见；底部 `.phone` 仍铺满全屏、壁纸不露白边，`ios-fs-active` 下 `.phone` 高度仍由 100dvh 填满（#109/#110 不复现）。【真机:待验证】（苹果16 添加到主屏幕+全屏）：桌面顶部应能看到 Mochi/时间/电量一行（iPhone 系统自带的状态栏会显示在其上方，形成上下两栏——用户已确认接受） |
| 112 | iPhone14 Safari 添加到主屏幕（standalone、**未开全屏**）：顶部被遮挡 | 根因（base.css）：`html.ios-pwa-standalone .phone` 这条普通 standalone 高度规则仍是裸 `height:100vh`——#109 的 `min()` 钳制只加了另外两条路径，漏了它。iOS standalone 下 `100vh`＝整屏高（iPhone14 实测 932px 含状态栏），而可视区（visualViewport）只有 873px → `.phone` 超出视口、`html,body` flex 居中把顶部推出负值（实测 top=-29、bottom=903）→ 顶栏/返回按钮被推出屏外点不到，底部也越界。修复：`height:min(100vh, var(--mochi-ios-h, 100dvh))`，与 #109/#110 同套 `min()` 钳制（`--mochi-ios-h`=syncVvFit 实测可视高、键盘期被摘除；不设时回退 100dvh），`.phone` 恒填满可视区不溢出 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 base.css 内唯一）：`html.tablet.ios-pwa-standalone .phone { height:min(100vh, var(--mochi-ios-h, 100dvh)`。诊断实证（用户三台真机报告）：iPhone13/17 standalone+全屏（ios-fs-active）top=0 正常、iPhone14 standalone 未全屏 top=-29 复现。待构建+真机（iPhone14 添加到主屏幕、不开全屏）：桌面/任意功能页顶部栏与返回按钮应完整可见可点；iPhone13/17 已由 #111 覆盖（未推送），推后一并复测 |
| 113 | 【诊断信息】打开时弹出手机输入法又收起 → 灰屏 | 根因（device.js）：诊断弹窗**打开即自动复制**——`copyText()` 会临时建隐藏 textarea 并 `ta.focus()`（#键盘触发）、800ms 后随元素移除又收起，手机上表现为「输入法弹起→收起 + 灰屏」；且手机剪贴板有字数上限，打开就自动写长诊断会被静默截断。修复：**取消自动复制**（删 `autoCopy` 及终态 `copied` 判定），打开只读文本不再碰剪贴板、不再 focus textarea；需要发给开发者时由用户点【复制】/【导出txt】自行触发（导出 txt 不受字数上限影响） | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 device.js 内唯一）：`打开诊断就自动写长文本会被静默截断`。验收：手机上打开设置→诊断信息 → 不再弹输入法、无灰屏；正文照常读出更新；点【复制】/【导出txt】仍可用 |
| 114 | iPhone 12 Pro Max Chrome / 主屏幕打开 + 全屏模式：桌面顶部「Mochi / 时间 / 电量」这一行与 iPhone 系统状态栏（时间/电量）**重叠**（用户实测：系统栏在上、模拟栏在下应成两栏，实际两行叠在一起） | 根因（base.css）：窄屏 `@media(max-width:900px)` 里的 `.statusbar { padding-top:max(calc(14px + env(safe-area-inset-top,0px)),14px) }`（特异性 0,1,0）被**后加载的同特异性全局规则** `.statusbar { padding:4px 4px 12px }` 覆盖 → 全屏态模拟状态栏的 safe-area 顶部留白全部失效、内容顶到 y=0，与系统状态栏重叠（探针实测 padTop=4px）。修复：新增 `html.ios-fs-active .phone .statusbar { padding-top:max(calc(14px + env(safe-area-inset-top,0px)),14px) }`（特异性 0,2,1 提权），iOS 全屏态恢复安全区留白，模拟状态栏整体下移到系统状态栏下方形成两栏（承接 #111「全屏保留状态栏」）；非全屏态仍走原有 349 行规则 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 base.css 内唯一）：`html.ios-fs-active .phone .statusbar { padding-top:max(calc(14px + env(safe-area-inset-top, 0px)), 14px)`。探针 `tools/tmp-fs.mjs`（standalone+ios-fs-active 态）：`.statusbar` 的 computed padding-top 应由 4px 变为 14px（模拟环境 safe-area=0 时 min 值；真机 safe-area≈47px 时为 61px）。【真机:待验证】（iPhone 12PM 全屏）：桌面顶部 Mochi/时间/电量一行应显示在 iPhone 系统状态栏正下方、不重叠；聊天页底部输入栏贴合底部不被遮挡 |
| 115 | 红米 K60 至尊版 + Edge 151（Android 16，视口 406×739 DPR3）：聊天页**输入栏打字不显示、始终空白**，导致无法发送消息（用户确认：任意文字都空白，不只重复短句）。诊断佐证：v3.26.380、键盘/锁残留三行全 `n/a`、聚焦元素 `div#chat-input.chat-input`、`default:chat-msgs` 142.6MB、帧率采样 18fps | 该机型无法远程复现，按「字没进来 / 进来被清 / 进来没画」三条独立路径各堵一处 + 决定性埋点收口。**A 进来被清（已证实的真实缺陷，chat.js）**：v3.14 防复活守卫（`_mClearTxt` + 200/800ms 迟到兜底 + input 监听）只比「框内内容是否与刚发送文本完全一致」，用户发完短句立刻用输入法**整段上屏**重打同一条（「好的」「在吗」这类回复必撞）→ 上屏瞬间被静默清空，表现正是「打字不显示」。修复：新增 `lastUserEditAt` / `clearAppliedAt` / `userEditedAfterClear()` 真实编辑闸门，由 `keydown` / `compositionstart` / `beforeinput`（`insert*`）三类活动打点，三处守卫（input 监听、[200,800] 兜底复查、`addMsg` 防重发）一律加判；无输入活动的内核迟到写回仍照清（v3.14 语义不退化），防双击连发仍生效。**B 进来没画（base.css）**：键盘期 `.phone` 被 `syncAndroidKb` 改 height + `alignSelf`、`_aPanComp` 写 `top`，而聊天输入栏是模板原生 contenteditable、**不走 ceConvert**，拿不到 `.ce-box` 那层合成层保护 → 重排/平移后文字画进失效旧层，DOM 有字、屏上空白（同款已证实手法见 chat-pages.css `.ta-add .ce-box:focus`）。补 `.phone .chat-input { will-change:transform }`（常驻，键盘平移前层就已存在）+ `.phone .chat-input:focus { transform:translateZ(0) }`，群聊 `#gc-input` 共用 `.chat-input` 类一并命中。**C 进来滚出视野（mobile-adapt.js）**：contenteditable 自身有内部滚动，键盘平移/收起后 `scrollTop` 残留会把文字裁出自家裁剪盒，框看着是空的。新增 `healEditableScroll()`（`scrollHeight` 不高于 `clientHeight+1` 且 `scrollTop>0` 才归零，多行真滚动不动），挂 `input` 捕获 + `nudgeInputVisible()`。**D 决定性埋点（device.js + mobile-adapt.js）**：诊断新增「聊天输入栏现场」一行（元素/聚焦/contenteditable/文本长/HTML长/内部滚动/颜色/底色/caret/opacity/visibility/fontSize/transform 是否提升/待清守卫/框 top-bottom/是否被键盘盖）＋输入轨迹环形缓冲（`xy-home-v2:__diag-inp`，focusin/compositionstart/compositionend/input 各记文本长与滚动，cap 8）＋`window.__mochiAndroidKb()` 安卓键盘探针并入 `mochiVvDiag().kb`（原先只有 iOS 有 kb，安卓报 `n/a`） | `tools/verify-chat-input-guard.mjs` **17/17**（①a-d 合成层：产物两条规则 + computed `willChange=transform` + CSSOM 里 `:focus` translateZ 规则 + 群聊输入栏同样命中；②a-d 整段 IME 上屏重打同一条短句不被吞；③a-b 无输入活动的迟到写回仍被清＝v3.14 语义未退化；④ 双击连点只落一条；⑤a-b 内部滚动残留归零 / 多行真滚动不误伤；⑥a-c 探针并入诊断 + 轨迹已记录 + 诊断行已接入；⑦ 全程无新增 JS 异常）。**双向反向对照已实测**：闸门强制 `return false`（≈修复前）→ ②c/②d FAIL（文本被清空）；强制 `return true`（等于关掉防复活）→ ③b FAIL。②c 特意走 composition 整段提交——逐键 ASCII 输入第一个字符就走进 else 分支摘掉 `_mClearTxt`，永远测不到吞字判据。哨兵 10 条（另回填 v3.14 `document.execCommand('selectAll', false, null)` 那条，此前整块零保护）。哨兵有牙已验证：删掉 `userEditedAfterClear` 定义行 → 构建 exit 1 并如实报「src 里也没有＝修复真丢了」。**【真机:待验证】验收（红米 K60 至尊版 + Edge）**：输入栏打字应正常显示、能发送；若仍空白请**再发一次诊断信息**，按新增两行一次定分支——「输入轨迹」`n` 恒 0＝字根本没进来；`n` 涨过又掉回 0＝进来被清（A 未覆盖的路径）；文本长>0 且 transform=已提升＝进来没画（B 的层提升不够，需换 `-webkit-backface-visibility` 等手法） |
| 116 | 【花园·工坊】做不了花艺配方的花（用户反馈）。排查确认合成主链路无 bug（headless 实测：材料够→按钮出现→点击扣料/记成就/发花束全通），真实问题是**缺料时零反馈**：材料不够的配方卡不渲染合成按钮、也不说明缺什么，库存为空时整页像"功能坏了"；需求行只显示目标量不显示持有量；「奇迹」（烈焰玫瑰+蓝玫瑰）等稀有花配方只能先得稀有种子→种出→收获进花朵库存才可合成，无任何获取指引 | 修复（garden.js renderCraft + garden.css，跨域改动见 WORKLOG）：①配方需求行改「已有/需求」格式（如 `🌹 2/3`），不足的一项标红加粗（`.rq.short`）；②缺料卡新增 `.recipe-lack` 提示行「材料不足，还缺 🌹×1、🌼×1（在花园种下并收获后可合成）」；③合成按钮常驻，缺料时置灰禁用（`disabled` + `[disabled]` 样式），材料齐即恢复可点——用户一眼看清"能做/缺什么/去哪补" | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 garden.js 内唯一）：`recipe-lack`。验证：node --check 过；headless 探针（种子库存 rose5/daisy4/sunflower1/lavender2）：初恋/热恋可合成、其余配方显示持有量与还缺数量、缺料按钮置灰不可点；【真机:待验证】：花园→工坊，缺料配方应显示红标持有量+还缺提示+灰按钮 |
| 117 | vivo X200s（V2458A）+ Edge 151：**本地音乐每次刷新后播放失败**，必须删掉再重新添加才能听（用户实测）。诊断实证：v3.26.376、点歌瞬间报 `资源加载失败 <audio> https://…/mochi/%7B%7D`（`%7B%7D`=URL 编码的 `{}`，即 `audio.src` 被赋成字符串 `'{}'`）、IDB `default:music-file:sm_…=33.0MB`（好文件还在）、交互轨迹点歌→报错→modal-ok（offerRemoveDamagedSong 连续失败弹「移出音乐库」） | 根因（music-player.js）：本地歌播放链只判「值非空」就建 `<audio>` 并 `audio.src = v`——历史版本曾把 Blob 经 JSON 序列化（`JSON.stringify(Blob)`→`'{}'`）写进 `music-file` 键，脏值 `'{}'` 常驻 localStorage；每次刷新同步路径（`store.get('music-file:'+id)` 直接读 LS 原串）都把它当有效数据喂给 `<audio>` → 解析成站内路径 `/mochi/{}` 刷加载失败；删歌重加能听是因为重传覆盖了脏值。修复四道：① `plausibleLocalValue()` 形状校验——本地值只认 Blob / ≥10 字符字符串（有效 dataURL 远长于此，超短串必为脏值），脏值一律不喂 `<audio>`；② 同步路径读到脏 LS 值**只清 LS 副本、继续落 IDB 读权威值**（好 Blob 在 IDB 时刷新直接能播，无需删歌重加）；③ `loadLocal` 确认脏值后 `purgeLocalFile()` 清脏存储（仅当值已确认非法才删；null/undefined 缺失态不动 IDB，防挂起超时把好文件误删）；④ `playLocal` 第二层 `validAudioSrc` 兜底 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 music-player.js 内唯一）：`function plausibleLocalValue(v) {`。验证：node --check 过、构建哨兵 193/193、哑哨兵 0、verify 10/10、verify-music-single-audio 15/15。【真机:待验证】（vivo X200s + Edge）：刷新后直接点本地歌应能播（IDB 好值路径）；脏值歌不再报 `%7B%7D` 且自动清脏 |
| 118 | 小米15Pro + Chrome：**邀请TA 管理页** ①「打字框变形，文字会超出框外」；②「分组和细分选项需要新增删除按钮和批量管理，打多了打错了无法修改」 | #118-a 打字框变形：`.ta-add` 添加表单的 select 用了 `ti-type tc-input` 但 CSS 没 `.ti-type` 规则（grep 确认），仅 `.tc-input` 生效给 `width:100%`，select 独占一行 + input 换行成 2 行布局（ta-ask 的 `.ta-type` 92px 同行布局更紧凑），窄屏视觉「变形」；同时页内 `#ti-search` / `#ti-batch` / 添加区 input 都是 `.tc-input` → 转 ce-box 后**没有 `.ta-add .ce-box` 那套 `will-change:transform` + `:focus translateZ(0)` 合成层保护**（聊天页键盘期 `.phone` 被 `_aPanComp` 写 `position:relative;top`、高度收缩，ce-box 文字合成层停在旧位不跟随布局 → 文字画在框下方/外，小米15Pro Chrome 既往实测复现族）。修：①`src/css/chat-pages.css` 新增 `.ti-type { flex:0 0 auto; width:92px }`（与 `.ta-type` 同款，添加表单 1 行排版 [select 92px][input flex:1][button]）；②新增 `.tc-input.ce-box { will-change:transform }`（全站 tc-input 输入框合成层保护，搜索/批量导入 textarea/邀请话术输入 一并受益）；③跨域 `src/js/mobile-adapt.js` 修 ceConvert ce-ghost 类别名泄露——原序先 `inp.classList.add('ce-ghost')` 再 `box.className = 'ce-box ' + inp.className` 致可见 ce-box div 继承到 ce-ghost 类别名（CSS 当前只对 input/textarea 生效未致视觉异常，但属逻辑 bug；未来加 div.ce-ghost 规则会误伤），改为先存 `origClass` 再 add。#118-b 编辑 + 批量管理：用户原话「打多了打错了无法修改」「分组和细分选项需要新增删除按钮和批量管理」——当前 `.ta-row` 只有 ✕ 删除没有 ✎ 编辑、分组/未分组的细分（猜拳/Pong/贪吃蛇/贴贴）也无批量入口。`src/js/ta-invite.js` 补：①✎ **编辑**——每条自定义邀请行加 `.ta-edit` 按钮（26px 圆形灰底，✎ 字符；系统预设项隐藏），点击 → openModal 预填当前 text → 确认后更新 `q.text`/`tiSave`/重渲染；②批量管理 **toggle**——mine 面板顶部 `.mg-grp-row` 加「批量管理」按钮（与「新建分组」同行，激活态背景翻黑），开启后每行切到「batch checkbox + 文本（无 toggle/edit/del）」、页面底部贴出 sticky `.ti-batch-bar`（已选 N 条 + 全选 + 删除 + 取消）；batch checkbox 双向同步 tiSelected + 全选态、删除走 openModal noInput/staticText 确认后批量 splice（降序避免错位）+ tiSave + 重渲染 + 自动退出批量模式；状态 `tiBatchMode:bool` + `tiSelected:Set` 局部、`switchTiTab` 切 tab 自动退出 | 哨兵 5 条（build.mjs FIX_SENTINELS）：`.ti-type{flex:0 0 auto;width:92px`（chat-pages.css 内唯一）/ `.tc-input.ce-box{will-change:transform`（chat-pages.css 内唯一）/ `class="ta-edit" data-idx`（ta-invite.js 内唯一）/ `tiBatchMode`（ta-invite.js 内唯一）/ `'ce-box ' + origClass`（mobile-adapt.js 内唯一）。验证：`node --check src/js/ta-invite.js` 与 `node --check src/js/mobile-adapt.js` 全过；构建后由构建者跑 build.mjs 哨兵 5 条全绿 + `npm run verify` 现有套件验证。【真机:待验证】（小米15Pro + Chrome）：① 邀请TA → 我的添加 → 任意分组添加区：select/input/添加 三件套应在同一行（不再分两行变形）；聚焦搜索/批量/添加任一 tc-input 输入框打字不再出现「字出界/文字与框分离」症状。② 点击 ✎ 应弹「修改邀请话术」模态预填当前文案，修改保存后立即生效；点「批量管理」应出现底部操作条，可勾选多条后一次删除（带「删除选中的 N 条自定义邀请话术？此操作不可撤销」确认），取消/全选/退出批量模式均正常；切 tab（系统预设 ↔ 我的添加）自动退出批量模式 |
| 120 | iPhone 13 Safari��iOS 18.6��standalone PWA�����������ݺ��ٵ������ݣ��������ݲ�ȫ����ʧ�ܶࡣ���ʵ֤��LS 109 �� 451.8KB��ta-choose 83KB/avatar-user 54KB �ȴ���� LS����IDB �����ϸ�� 1 ����ѡ default:chat-msgs��**�ֿ�/�ظ�/�ղ���ϸ��LS 0�� 0B + IDB 0��**��cc-groups/quote-cards/fav-msgs/reply-* ȫ���� | ���������� �ֿ���(cc-groups)/�Զ����ֿ�(quote-cards)/�ղ�(fav-msgs)���� IDB-only ����>200KB ʱ xyStore.set ���� LS ֻд IDB+memoryCache������ ����ʱ idbGet ����Щ����iOS Safari IDB �������/��ʱ��8s ���ޣ��� ���� undefined���� ��������ֻ�� chat-msgs/feed-posts��isAuthorityKey����¼����(exportMissing)��**��������Ĭ����**�������ļ�ȱ��Щ��������ʾ"ȫ����������"���� ����ʱ idbReplaceAll �� os.clear() ��� IDB �� put �� ȱʧ�ļ����׶�ʧ���޸���data-backup.js ���ࣩ��**������ A** IDB ��ȡʧ�����Դ� 1 ������ 3 �Σ���� 200ms �� armFgIdbReset �ؽ����ӻָ����ᣩ��**������ B** ȫ����ʧ����¼ exportMissing��ԭֻ isAuthorityKey��+�Ѻ����� nameOf��cc-groups/quote-cards/fav-msgs/avatar-*/music-file/reply-*/ta-* �ȣ�+��ʵ��ʾ"�������ԭ�豸���ݣ�������������������µ���"��**����� C** idbReplaceAll ǰ���г���ǰ IDB �����ҳ�����û�еļ����Ҳ��� data.ls��������ֵ���� pairs �� clear ����Щ��Ҳ�� put ��ȥ �� �������ݡ���������ʱ������Ϊ�գ��������岻�䣨�滻����ֻ�б��ݲ�����ʱ�ű����ɼ����ϲ������ȶ����ݰ�ȫ | �ڱ� 4 ����build.mjs FIX_SENTINELS��needle ���� data-backup.js ��Ψһ����`for (let retry = 0; retry < 3 && (v === undefined || v === null); retry++)` / `���ڶ����ж�ʧ����¼������������ʵ��ʾ�û����ݲ�����` / `TA�ظ��ֿ�(` / `�ѱ�������δ����`����֤��`node --check src/js/data-backup.js` ���������������ڱ�+verify+������գ�iPhone / Safari ������������ֿ�/�ظ�/�ղ�Ӧ�ڣ� |
| 121 | iPad Air 7 + Safari（standalone PWA）：通话中刷新/关掉重开网页后通话不恢复（「通话设置→刷新后恢复通话」开着也没用），也不记通话中断 | 根因：通话进行中标记 `xy-home-v2:call-active` 只存 sessionStorage——同标签普通刷新能活，但关标签页/Safari 后重开、iPadOS 杀后台后重开（主屏幕 PWA 重开同此）sessionStorage 整体清空，call.js recoverCall() 读不到标记，既不恢复也不记中断。修复（src/js/call.js 四点 + src/js/contacts.js 一点）：①双写 localStorage 同键兜底（saveCallActive/clearCallActive 两存储同步）；②接通后 durationTimer 每 20 秒心跳刷新 ts（新鲜度判定依据）；③recoverCall sessionStorage 优先、为空读 localStorage 兜底，LS 命中且 ts 距今 ≤10 分钟才恢复（超窗静默清标记防翻旧账），恢复成功回写 sessionStorage 走快路径；④resume 关闭路径照旧记中断记录（LS 兜底后连「关浏览器重开」场景也能补记，旧方案这条会丢）；⑤call-active 加进 contacts.js migrateLegacy 排除清单（探针实测抓到：每次启动 migrateLegacy 把不带命名空间的全局根键当旧顶层键迁进 default 并删根键，LS 兜底副本启动即被搬走——同 fish-log/incoming-requests 既有先例） | 哨兵 4 条（含 contacts.js 'call-active'];）（build.mjs FIX_SENTINELS，needle 均在 call.js 内唯一）：`localStorage.setItem(CALL_ACTIVE_KEY, payload)` / `localStorage.getItem(CALL_ACTIVE_KEY)` / `if (++hbCount >= 20) { hbCount = 0; saveCallActive(); }`。验证：`node --check src/js/call.js` 过；构建后哨兵全绿 + `npm run verify`。【真机:待验证】（iPad Safari）：通话接通→等 20 秒以上→杀掉 Safari/主屏幕 PWA 重开→通话小框应恢复且时长从接通时刻延续 |
| 122 | 字卡库搜索搜不到系统编码字卡（TA的心情 235 张/贴贴回应/邀请婉拒/朋友圈TA评论/番茄钟陪伴/群聊兜底等系统预设池未注册搜索源） | 各功能内置系统字卡池此前只有部分（查岗/邀请/情话/位置/寻踪/TA询问族）注册了 `window.__cardSearchFns` 跨分类搜索，TA_MOOD_DATA 235 张及若干内置回应池游离在外。补注册 5 个只读搜索来源：ta-mood.js「TA的心情」（cat=分组名，已关卡片带 ·已关 标记）、chat.js「聊天系统回应」（兜底/游戏邀请婉拒/贴贴婉拒/贴贴回应）、feed.js「朋友圈互动」（TA评论/TA回应回复）、p2-features.js「番茄钟陪伴」（开场/鼓励/完成/回应/累了五池）、group-chat.js「群聊系统回应」（兜底回复）；不改任何抽取/存储逻辑 | 哨兵 5 条（build.mjs FIX_SENTINELS，needle 均在各自 file 内唯一）：`name: 'TA的心情'` / `name: '聊天系统回应'` / `name: '朋友圈互动'` / `name: '番茄钟陪伴'` / `name: '群聊系统回应'`。验证：`node --check` 五文件过；构建后哨兵全绿；字卡库列表页搜索「心情平静」等关键词应命中 TA的心情 来源 |
| 123 | 备份提醒条从未出现（数据只存本机，iOS Safari 会系统级清空 localStorage+IDB，唯一防线是定期导出备份） | pwa.js 定期备份提醒：`backup-remind-bar` 顶部提醒条——距上次成功导出（`__last-backup`）超 7 天且近 7 天未提醒过（`__last-backup-remind`）才显示，版本更新条优先显示时让位；`mochi-restore-done` 数据就绪后判定，`contacts` 键不存在（全新安装/空状态）不提醒；文案区分「已备份 N 天」与「从未备份」；点【去备份】走 `runBackupExport` 正常导出流程 | 哨兵 `backup-remind-bar`（build.mjs FIX_SENTINELS，js/pwa.js + template.html 双登记）/ 验证：删除 `xy-home-v2:__last-backup` 后刷新 → 顶部出现提醒条；导出一次后 7 天内不再出现 |
| 124 | OPPO Find X9 + Chrome：打开网站容易崩溃、打开聊天也容易崩溃（低端机内存不足）。诊断实证：v3.26.385，LS `default:chat-msgs` 155MB、`default:cc-groups` 40MB——冷启动/切桌时同步预读数个超 8MB 大包，低端机一次性把 155MB 聊天历史读进内存直接 OOM崩溃 | 根因（chat.js）：冷启动与切桌路径无条件 `loadMsgs()` 预读当前桌面聊天历史，历史越大内存峰值越高（155MB 单桌面足以让低端机 OOM，开聊天再叠加渲染更易崩）。修复「大历史懒读」：①`chatLedgerSave` 落账本时把「已落盘字节估算」写成元数据 `b` 字段（内存缓存 dedupe 防重复写），`chat-meta` 账本由此携带历史大小信息；②新增 `chatPrefetchIfLight()` 门控——启动/`mochi-restore-done`/切桌三处预读入口统一改为先读账本 `b`：`b` 已知且 ≤ 8MB（`CHAT_LAZY_BYTES`）才预读（小历史保持快开），大包/未知大小一律跳过冷启动预读，等用户真正进入聊天页再读（此时是单一交互负载，比开屏时钟点更可控）；数据零风险：只推迟读取时机，不改任何持久化/合并/防丢逻辑，`b` 缺失按「未知」走保守跳读不破坏旧行为 | 哨兵 2 条（build.mjs FIX_SENTINELS，needle 均在 chat.js 内唯一）：`function chatPrefetchIfLight(load) {` / `const chatLedgerBytes = {};`。验证：`node --check src/js/chat.js` 过；构建后哨兵全绿 + `npm run verify`。【真机:待验证】（OPPO Find X9 Chrome）：开屏不预读大聊天包、进入聊天页正常加载历史、不再崩溃 |
| 126 | 新功能（v3.30.x）：公用字卡 / 专属字卡里想「关闭某个分组的使用」——字卡不删，但该分组不再被联系人自动回复/拍一拍/表情包/语音/朋友圈/信箱/群聊/TA 主动分享等任何池使用，且随时可重新开启 | chatcard.js 分组停用开关：①数据层新增独立键（不污染 cc-groups 结构）：公用 `xy-home-v2:cc-groups-public-off`（全局根键，已进 contacts.js EXCLUDE 防 migrateLegacy 迁走）/ 专属 `<cid>:cc-groups-off`，格式 `{分类:[分组名]}`——同名分组按分类区分，停用专属某组不影响公用同名组；②管理页每个分组 header 右侧新增眼睛开关按钮 + 停用态 `.off` 样式与「已停用」标签（render 与 rebuildGroupAfterRemove 两处 header 构建共用 `groupHeaderHtml/bindGroupToggle/refreshGroupHeaderUI`）；③回复池过滤：getCustomCards/getPokeCards/getPokeGroups/getMediaCards/getMediaGroups/getCustomCardsFor/getPokeCardsFor/getMediaCardsFor 与 warmShrunkCache 统一改走新合并视图 `replyPoolGroups/replyPoolGroupsFor`（`mergeFiltered`：专属/公用各自先 `filterGroupsByOff` 再 concat），getScopedGroups 面板同步过滤；管理页搜索/导出/统计等管理视角不受影响（仍走原 mergeWithPublic）；④切换联系人 `offInvalidate` 失效专属停用缓存 | 哨兵 2 条（build.mjs FIX_SENTINELS，needle 各自 file 内唯一）：`const PUB_OFF_KEY = 'cc-groups-public-off';`（chatcard.js）/ `'cc-groups-public', 'cc-groups-public-off', 'cc-scope-migrated',`（contacts.js）。验证：`node --check` 三文件过；`node tools/verify-cc-group-off.mjs` **12/12**（header 开关 / 停用专属剔除回复池 / 同名公用保留 / 面板 getScopedGroups 过滤 / 公用独立停用全局生效 / 重新启用恢复）；构建哨兵我的 2 条在位。另修一条既有哨兵：删除型 `ta.focus();` needle 与 chat.js 等 4 处合法 `ta.focus()` 撞车必然误报（ZCode 登记时未构建验证过），收窄为 `appendChild(ta);ta.focus();`（device.js copyText 上下文唯一） |
| 127 | 手机上点【发送】按钮发消息后输入法自动收起，连续对话每条都要重新点输入框弹键盘（用户反馈） | 根因：点按按钮的 `mousedown` 默认行为会把焦点从 contenteditable 输入框抢到按钮上，移动端输入法随即收起（输入框内容清空/re-render 后焦点未回）。修复（chat.js 单聊 + group-chat.js 群聊同款）：①发送按钮 `mousedown` preventDefault 阻止焦点转移（键盘不收）；②`click` 发送后 `input.focus()` 回焦兜底（部分内核 click 路径仍会失焦）；Enter 发送路径本就有焦点，不受影响 | 哨兵 2 条（build.mjs FIX_SENTINELS，`send.addEventListener('mousedown'…preventDefault` chat.js 内唯一 / `sendBtn.addEventListener('mousedown'…` group-chat.js 内唯一）。验证：`node --check` 两文件过；构建哨兵在位。【真机:待验证】：手机聊天/群聊连发多条消息，键盘应保持弹出不再每条收起 |

| 128 | iPhone 12 Pro + Safari（多角色，一直开启跨桌面查岗/打电话，频率「标准」）：最近两三天跨桌面查岗与来电一次都没触发（附诊断信息 v3.26.387） | 根因（contacts.js）：v3.26.x 新增的频率档位键 `desk-freq-mode` 遗漏在 EXCLUDE 列表之外，`migrateLegacy` 每次启动把它当「旧顶层业务键」迁进 default 桌面并删根键 → `incoming-requests.js deskFreqMode()` 读根键空，静默回退默认「安静」（1% 概率 + 3h 冷却），用户选的「标准」（2%/30min）失效。叠加 iOS Safari 浏览器（standalone=false）后台 setInterval 停摆、跨桌面来电额外要求前台（!document.hidden）才掷概率 → 实际掷概率窗口极小，两层叠加＝两三天 0 次。修复：① EXCLUDE 列表加 `desk-freq-mode`（防未来再迁移）；② migrateLegacy 的「误迁自愈」数组同列并入 `desk-freq-mode`（把已误迁进 default 的副本写回根键，存量一次性找回，幂等：根键已有值不覆盖）。临近的冷却键 `incoming-last:checkin|call:<cid>` 含冒号走 isExcluded「命名空间键保守不迁移」分支，本就安全，未动 | 哨兵 2 条（build.mjs FIX_SENTINELS，均在 contacts.js 内唯一）：`'desk-call-en', 'desk-freq-mode'`（EXCLUDE 根因）+ `'hide-ta-sticker', 'desk-freq-mode']`（自愈数组）。验证：node --check contacts.js/build.mjs 过；node build.mjs --check-sentinels 我的 2 条锚点全部在位（总 241 条；另 7 条红为 #125 base.css 已知并发回归，非本次引入）。待构建后真机：iPhone 12 Pro Safari 设置里频率确为「标准」后，跨桌面查岗/来电恢复按 2%/30min 触发 |
| 129 | iPhone（苹果×手机）自带 Safari 主屏幕打开（standalone PWA）：桌面组件显示不全、竖滑滑不到底（底部被 Home 指示条遮挡，能滑但看不见底部导航/组件） | 根因（mobile-adapt.js syncSafeBottom）：`screen.height - innerHeight > 60` 判定本意识别「浏览器工具条占空间」（标签页模式）→ 归零 `--mochi-safe-bottom`。但 iOS standalone 下该差值（诊断实测 screen 956 − innerHeight 894 = 62）是系统状态栏/Home 指示条而非浏览器工具条，且 standalone + viewport-fit=cover 下 Home 指示条在可视区内，归零让 tabbar 等底部组件不避让被 Home 指示条遮挡（tabbar 固定不随 page-slide 滚动，故「滑不到底」）。修复：归零条件加 `!ios-pwa-standalone` 守卫，standalone 下摘除属性让 CSS 回落 `env(safe-area-inset-bottom)` 正确避让 Home 指示条 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle 在 mobile-adapt.js 内唯一）：`sh - ih > 60 && !d.classList.contains('ios-pwa-standalone')`。验证：node --check 过；standalone 下 --mochi-safe-bottom 摘除、CSS 回落 env()。【真机:待验证】（iPhone 主屏幕打开）：桌面底部导航/组件不再被 Home 指示条遮挡，完整显示可点 |
| 131 | 小米15Pro Chrome：邀请TA 管理页「我的添加」里输入栏打字时文字显示在输入框下方/外面（框移新位、文字停在旧位，合成层字出界） | 根因（ta-invite.js）：ta-invite 管理页的 .ta-add 输入框转 ce-box 后，安卓键盘弹起（interactive-widget=resizes-content）致 layout viewport 收缩 → page-ta-invite 重排 → ce-box 文字合成层停在旧位置。ta-ask.js 有 _reflowAskCeBoxes（监听 vv/window resize 防抖刷新 .ta-add .ce-box 合成层）缓解此族问题，ta-invite.js 漏了同款缓解。mobile-adapt.js 的 _aRefreshCe 虽扫描全站 .ce-box，但有 _aUserTypos<500 闸门（敲键时跳过），敲键中重排场景覆盖不到。修复：ta-invite.js 补 _reflowInviteCeBoxes（同 ta-ask.js 实现：监听 visualViewport.resize/window.resize，120ms 防抖，仅 page-ta-invite 可见时对 .ta-add .ce-box toggle transform+reflow 强制合成层重新提交位置） | 哨兵 1 条（build.mjs FIX_SENTINELS，needle pg.querySelectorAll('.ta-add .ce-box') 在 ta-invite.js 内唯一）：验证：node --check 过；node build.mjs --check-sentinels 244 条全绿、我的锚点在位。【真机:待验证】（小米15Pro Chrome）：邀请TA→我的添加→点输入框打字→文字在框内正常显示不再出界 |
| 132 | 邀请TA「我的添加」批量管理只能批量删除，选中多条后无法一键移动到别的分组（字卡库 chatcard 已有「移动」按钮，ta-invite 缺） | 根因（ta-invite.js）：v3.26.x #118 加批量管理时只做了「全选/删除/取消」三条，漏了 chatcard.js 有的「移动到分组」。修复：①批量条 HTML 加「移动」按钮（删除前、取消前，disabled 态随选中数同步）；②bindTiBatchBar 加移动逻辑——点击弹 openModal pills 选择目标（未分组 + 现有分组），确定后遍历 tiSelected 改 q.grp 字段（空=未分组 delete q.grp），保存后退出批量模式 + 重渲染 + toast 提示移动数和目标组名；③行内 checkbox 切换时同步移动按钮 disabled 态 | 哨兵 1 条（build.mjs FIX_SENTINELS，needle id="ti-batch-move" 在 ta-invite.js 内唯一）：验证：node --check 过；node build.mjs --check-sentinels 244 条全绿、我的锚点在位。【真机:待验证】：邀请TA→我的添加→批量管理→勾选多条→移动→选目标分组→选中条移到该组、数量正确、toast 提示 |